# Q71 卡內分組三級制 — 全站盤點（進行中）

工作目錄：`Project/ztor-creator-studio/site/r2.2/`
規則來源：`STYLE-DECISIONS.md` Q71（分組三級制）／Q72（列線 `--border-soft`、組線 `--border`）／Q66（巢狀往亮疊）／Q74（單選卡維持橘點畫法，不動）；`demo-eshop-styles.html:1207-1218`「已定案」5 條。

狀態：批次 A（6 頁）已完成，批次 B／C／D（16 頁）掃描中，本檔會持續更新到 22 頁全數完成。

---

## 跨頁共通發現（元件層，影響多頁，非單頁自寫問題）

**Q72 列線色階，兩支元件檔尚未跟進裁決**（`kv-list.css` 已修好，這兩支還是舊值）：

- `ds-components/data-list.css:36` — `.data-list__row { border-bottom: 1px solid var(--border) }`，這是清單「列與列」之間的線，該改 `--border-soft`。同檔 `:54` 的 `.data-list__row--child + .data-list__row:not(.data-list__row--child)` 也一樣。級別：**明確可改**（元件層一次修好，全站消費點跟著對）。
  - 命中頁（本輪 22 頁範圍內，`data-list__row` 出現次數 >0）：admin-video-review(1)、auction-detail(15)、bundle-detail(7)、create-bundle(12)、create-project(22)、creator-detail(3)、manage-ip(2)、order-detail(9)、pickup-detail(8)、project-detail(26)、register-ip(2)。
- `ds-components/table.css:60` — `.ztor-table tbody td { border-bottom: 1px solid var(--border) }`，同樣是列線，該改 `--border-soft`。級別：**明確可改**。
  - 命中頁：admin-platform-fees(29)、admin-video-review(2)、pickup-detail(2)、project-detail(66)。
  - 例外：`admin-platform-fees` 的「費率樹」（`.fee-tree__panel .ztor-table`）已在 2026-07-22 把列線關掉（`border-bottom:0`），不受影響；只有「版本歷史」「費率例外」兩張標準表格受影響。
- `ds-components/settings.css:58-63` — `.settings-row { border-bottom: 1px solid var(--border) }`，開關列與列之間的線，該改 `--border-soft`。級別：**明確可改**。
  - 命中頁（本輪範圍內）：manage-ip.html（租賃紀錄的開關列）。

這三處建議另開一張「Q72 元件層補漏」工單，一次修元件檔，比逐頁改動效益高；本檔以下逐頁清單仍照原派工範圍，只列「頁面自寫」的分隔線問題。

**Dimension C 根因（跨頁重複出現，建議一次性 token 修法）**：`ds-components/_tokens.css:62` 淺色模式 `--input-surface: var(--card)`（兩者同為 `#FFFFFF`），而 `ds-components/chip.css` 的無框化（`.chip { border:0; background:var(--input-surface) }`）**已經上線、不是「即將」**。任何未選中的素色 `.chip` 只要坐在 `--card`／`.form-section--outlined` 背景上，淺色模式下就是同色疊同色、零邊框——藥丸形狀整個消失、只剩文字浮在版面上；深色模式因 `--input-surface`（`#2A2B2D`）與 `--card`（`#212223`）有色差不受影響。這個根因在批次 B 命中至少 5 個頁面 6 處，建議優先做「淺色模式 `--input-surface` 與 `--card` 拉出一階色差」這個 token 級修法，會一次解掉多數 Dimension C 發現，比逐頁補框有效率。

**Dimension A 前提缺口**：Q71 第 1 級「小標＋滿版髮絲線」（G-B）目前只存在於 `demo-eshop-styles.html` 的頁面內 CSS（`.gsub`/`.gb-div`），**尚未 promote 進 `ds-components/` 或寫進 `design-system.md`**（全庫搜尋 `gsub`/`gb-div` 在 22 頁範圍內 0 命中）。這代表「明確可改」的第 1 級替換要真正批次施工前，得先把 G-B 做成可複用元件（例如併入 `card.css` 或新開 `group-divider.css`）；在那之前，各頁現有的「用 h3/h4＋各自手寫 border-top」只能先標「需人判斷／待元件落地」，不宜逐頁各自複刻一份新 CSS。

---

## 批次 A（已完成）

### admin-ip-bank-entry.html
合規、無發現（僅 21 行的簡易新增表單，無卡內分組、無多選、無 chip、無清單列）。

### admin-platform-fees.html
- 見上方跨頁共通發現（`table.css:60`），本頁另無自寫分隔線／灰底問題。
- A/C/D/E 四維度：無發現。

### admin-video-review.html
- 見上方跨頁共通發現（`data-list.css:36`、`table.css:60`）。
- A/C/D/E：無發現。審核佇列是單筆審核流程，非多選場景。

### auction-detail.html
- 見上方跨頁共通發現（`data-list.css:36`，Bids／Auction info 兩分頁的列）。
- A/C/D/E：無發現。`.ad-hero`/`.ad-gallery` 的 `background:var(--muted)` 是圖片佔位灰底，正常用法，非分組填色層違規。

### bundle-detail.html
- 見上方跨頁共通發現（`data-list.css:36`，成員清單 `#bd-members`）。
- A/D：庫存版本用 `.selection-card`（單選 Unlimited/Limited）——正確用法（頁內註解已標明「非 radio-card」，符合 Q74），不動。
- C/E：無發現。

### create-auction.html
- `create-auction.html:323-328`（`#ca-tags-suggest` 的 `.chip-group`）— 現況：建議標籤 chip 坐在 `form-section`（背景 `--card`）上；`ds-components/chip.css` 今天（2026-08-25）已無框化（`.chip { border:0 }`），而 `_tokens.css:62` 在**淺色模式**下 `--input-surface` 等同 `--card`——即淺色模式下 chip 底色會跟卡片底色完全同色、邊界消失。深色模式因 `--input-surface` 另外調亮不受影響。級別：**需人判斷**（token 設計取捨，非本頁單獨能修，影響全站所有淺色模式下坐在卡片背景上的 `.chip`，建議與 chip 無框化那張工單一起處理）。
- D：Tags 建議標籤已是 MS-B（`.chip`/`.chip--active`）正確用法，不必替換。
- A：拍賣種類（實體/數位/活動）、時長（3/5/7 天）都用 `.selection-card` 單選，正確（Q74），不動。
- E：「上架設定」「開拍設定」用 `.radio-list--collapsible`（`ds-components/radio-list.css` 有專門設計：框線、hover、chevron），不是陽春密集堆疊列，不算違規。

**批次 A 小計**：明確可改 2 處（跨頁元件層 `data-list.css`／`table.css`，各自命中多頁）＋需人判斷 1 處（create-auction chip／`--input-surface`＝`--card` 淺色模式邊界消失）。

---

## 批次 B（已完成）

### create-bundle.html
合規、無發現。`.cb-add-tile`/`.cb-summary` 已依 Q66（2026-08-13）改用 `--nest-surface`/`--nest-line`，無 chip/checkbox；單選用的 `selection-card`/`radio-list` 用法正確。

### create-campaign.html
- `create-campaign.html:44,222-229` — `.fc-calc { background: var(--muted) }`，用於「每股金額」「每股股權」兩顆唯讀計算格，坐在**非 outlined 的一般** `.form-section` 裡。深色模式下 `--muted` 是 `#161718`，正是 Q66/Q71 已否決的「往暗陷」絕對暗色填色。級別：**需人判斷**（是否真的算「巢狀分組」情境、該降級成排版或改 `--nest-surface`需設計裁決；也可能只是唯讀數值格的既有慣例，不在 Q71 三級制範圍內）。

### create-event.html
- `create-event.html:449`（`#ce-lineup-list` `.chip-group--loose`）＋ `:5026-5027`（`renderLineupChips` 產出 `.chip.chip--removable` 無 `--active`）— 坐在 `.form-section--outlined`（卡背景）內，屬 Dimension C 根因（見上方）。級別：**需人判斷**。
- `create-event.html:4606-4608`（ticket-tier modal「加購商品」chips）— 同一根因、同樣坐在卡背景上。級別：**需人判斷**。
- Level-3 巢狀（場次/系列卡，:3030-3064）已依自身註解正確改用 `.card--muted`（Q66 2026-08-25），合規。
- `.kv`/`kv-list.css` 列線色已修好，頁面無覆寫，合規。

### create-event-legacy.html
合規、無發現。無 chip/checkbox；`session-list.css` 元件（日期列）無填色層、無列線誤用；單選控件維持原樣正確。

### create-product.html
- **Q71 第 2 級正例（尺寸指南）**：`:253-276` 用 `.control-group.control-group--plain`（`box-shadow: inset 0 0 0 1px var(--border)`，1px 線框無填色）— 合規，正例成立。
- **Q71 第 3 級正例（多規格建構器）**：`:394` `<div class="variant-builder nest">` 正確用 `.nest` — 合規，正例成立。
  - 但 `:393` 的行內註解已過時：仍寫「疊在卡片上的一層（滿版貼齊卡底＋向上陰影）」，但 `ds-components/nest.css` header 已明載 2026-08-25 裁決拿掉向上陰影、改用上緣髮絲線，`--shadow-nest-up` 這個 token 全庫已不存在。級別：**明確可改**（純文字註解更新，機械式）。
- `:738-742`（`#cp-tags-suggest` chip-group，5 顆未選建議標籤）坐在 `.form-section--outlined` 卡背景上，屬 Dimension C 根因。級別：**需人判斷**。
- 其餘（規格、媒體、折扣、限購、上架模式）皆用排版或既有 `.control-group`/`.radio-list`，無 A/B 違規；`:91` 的 `.cp-shopmock__thumb/hero` 用 `--muted` 是圖片佔位灰底，非巢狀分組層，不算違規。

### create-project.html
- `:215-217` — 格式 checkbox（Digital/Vinyl/CD）用鬆散 `.zcheck` label＋flex-row，3 個選項無副標。屬 MS-B（`.chip.chip--active`）候選。級別：**需人判斷**（3 個選項是否夠「多」到符合 MS-B 判準需設計裁決，非陽春垂直堆疊，不算維度 E）。
- `:406-409` — 廣告版位 checkbox（Pre-roll/Mid-roll/Post-roll/Banner）4 欄 grid，同樣的 MS-B 候選場景。級別：**需人判斷**。
- `:199,233`（`.chip-group[data-taxonomy-genres]`，`js/work-taxonomy.js:62-64` 產出）已經正確用 MS-B 的 chip/chip--active 切換（Dimension D 不用改），但未選態坐在 `.form-section--outlined` 卡背景上，屬 Dimension C 根因——21 顆題材 chip 未選時淺色模式下彼此與底色融成一片。級別：**需人判斷**。
- 單選（access tier :348-361、publish timing :436-442、gate :70-73）維持原樣正確，Q74 不動。

**批次 B 小計**：明確可改 1 處（create-product.html:393 過時註解）＋需人判斷 8 處（campaign `.fc-calc` 灰底 1、event chip 場景 2、product tag-suggest chip 1、project checkbox→MS-B 候選 2、project genre chip 對比 1，另計 1 為 batch B 自行拆分計數，詳見逐頁清單）。

## 批次 C（已完成）

### creator-detail.html
合規、無發現。

### manage-ip.html
合規、無發現（`.settings-row`/`.zcheck`/`chip--static` 都在既有元件正常用法內；chip 坐的底色是 `--card`/`--surface-shell`，跟 chip 本身的 `--input-surface` 不同色，無框化後不會融邊）。另見上方跨頁共通發現（`settings.css:58-63` 開關列列線、`data-list.css:36` 租賃紀錄）。

### order-detail.html
- `order-detail.html:573-576` — 部分退款「勾選要退的品項」用 `.data-list__row` + `.zcheck__control`，checkbox 放在列**最左側**（標籤在其右邊）。這正是 MS-A（清單列＋右側核取）的語意場景（選項少、有副標／金額、直向清單），但目前 checkbox 在左不在右，跟定案的「標籤靠左、方塊靠右對齊成一欄」相反。級別：**需人判斷**（MS-A 尚未 promote 成 `ds-components` 正式元件即 `.ms-row`，要嘛先等 MS-A 落地、要嘛先手動把 checkbox 移到列尾）。
- 其餘（`.od-items__head`／`.od-amt-group` 的組線）是「表頭↔內容」「品項清單↔金額拆解」的組線，非列與列，Q72 判定正確，不算違規。
- 另見上方跨頁共通發現（`data-list.css:36`，品項/退款/快照列）。

### pickup-detail.html
合規、無發現（kv、product-list、data-list、tabs、filter-tabs 皆用既有元件，沒有多選、垂直單選或自製灰底）。另見上方跨頁共通發現（`data-list.css:36`、`table.css:60`）。

### product-detail.html
合規、無發現。逐一核對：
- VIP 卡「Text／Image」用 `segmented.radio-cards` 單選，正確維持 Q74 畫法，不用動。
- 上架設定用 `.radio-list--collapsible`（三選一）：站上刻意設計、有 Figma 出處的獨立元件家族，不屬於維度 E 該換 `radio-card--stack` 的對象。
- 電影關聯（`partials/film-picker.js`）已是 `.chip`/`.chip--active` 的搜尋式多選，本來就對齊 MS-B，不需改。
- 「Enable discount」「Limited-time discount」「每人限購」用 `.control-row`/`.control-group`（inset 線框、無填色），正是 Q71 第 2 級官方做法。
- 全頁 chip 出現處底色都跟容器不同色，無框化後不會融邊。

**批次 C 小計**：明確可改 0 處（跨頁元件層問題已併入上方共通發現）＋需人判斷 1 處（order-detail 退款品項核取框左右位置）。

## 批次 D（已完成；子 agent 產出，並已與主對話自行直接查核的結果比對合併）

### project-detail.html
- **`project-detail.html:713-715`（交付摘要的兩張 KPI 卡）— 明確可改**：頁內註解明寫「底色走 --nest-surface（.card--muted）」，`ds-components/kpi.css:225-229` 也已備好 `.kpi.card--muted` 組合選擇器，但實際 HTML（`class="kpi bento--span-4"`）與渲染 JS（約 3332-3339 行）都沒有真的加上 `card--muted`——現在吃的是 `.kpi` 預設的 `var(--card)`，跟外層卡同色，第三級疊層沒有做出來。該改成：在這兩個 `data-prog-kpi="…"` 的 `.kpi.bento--span-4` 補上 `card--muted` class。
- `project-detail.html:528-533`（`#pd-tags-suggest` chip-group，5 顆未選建議標籤）坐在卡背景上，屬 Dimension C 根因（見上方 `--input-surface`＝`--card` 淺色模式融邊）。級別：**需人判斷**。
- `:591` 的 `border-top:1px solid var(--border)`（Auto full refund 開關列上緣）是「排程表格區塊→退款開關」的組轉換邊界，非同組列與列，Q72 判定正確，不算違規。
- 另見上方跨頁共通發現（`data-list.css:36`、`table.css:60`，本頁命中最多：26 處 `data-list__row`、66 處 `ztor-table`）。
- 其餘未發現多選誤用；未發現陽春垂直單選清單。

### register-ip.html
- **`register-ip.html:208-217`（Permitted usage，`#ri-usage`）— 明確的元件誤用**：用單選家族 `.selection-card`＋`.selection-grid`，但 JS 接線在 `:760-761` 是 `classList.toggle("selection-card--active")`（不移除同組其他已選、可同時多個 active），資料收集在 `:518` 是 `Array.from(...).map(...)` 收集所有已選，區塊副標 `:209` 也明寫「Select at least one」——語意就是多選。對照同頁 `#ri-type-grid`／`#ri-person-grid`／`#ri-list-grid` 都是「先清空全部 active 再選一個」的正規單選寫法，`#ri-usage` 明顯不同掛。5 個選項各帶副標說明、需要一次看完，貼近 MS-E（兩欄卡片＋圓形標記，Cosmos 版型）判準。現況：無論選幾個，都只有 Q74 的橘點/outline 表達，選了 3 個跟選了 1 個一樣不明顯。級別：判級錯誤本身**明確**（單選元件不能承載「至少選一、可複選」語意），但實際替換施工要等 MS-E／`check-card` 正式 promote 進 `ds-components`（目前只在 demo），且是否要分組、要不要帶 `.cos__count` 需設計判斷——故整體列**需人判斷**。
- `register-ip.html:225-234`（Territory，`#ri-territory`）已經是 MS-B 正確用法（`.chip`/`.chip--active` 多選 toggle，`:768-776`），不必替換；但未選 chip 坐在 `.form-section--outlined` 卡背景上，屬 Dimension C 根因。級別：**需人判斷**。
- `:102-107`（IP 類型）、`:122-124`（Person-based 子類型）、`:262-263`（Rental/Private）三組 `.selection-card` 都是互斥單選，正確維持 Q74 畫法，不動。
- `:251,280,289` 的 `.zcheck` 是個別性質的單顆核取（非成組多選清單），屬 MS-D 語意，不算 MS-A/E 替換對象。
- Dimension B：未發現頁面自寫分隔線問題。Dimension E：未發現垂直單選清單。

### series-detail.html
合規、無發現。特別核對：逐張票種用 `form-section--outlined` 疊在同一層 outlined 區塊裡（`:735`），看起來像卡中卡，但 `ds-components/form-section.css:56-59` 的 Q66 全站規則（2026-08-13 已落地，註解點名「系列詳情的場次」正是當時被修過的 6 處之一）會自動把內層翻成 `--nest-surface`/`--nest-line` 薄膜，非違規；`.sd-tickets__bar` 的 `var(--muted)`（`:44`）是進度條軌道底色，屬控件填色、非分組疊層，非 Q71 範圍。

### e-shop.html
合規、無發現。頁面裡沒有真正渲染出來的 `.chip`（grep 命中的 4 處全是 JS 變數名或註解），狀態篩選用的是 `.filter-tabs`（單選 tabs，非 chip、非多選家族），沒有 checkbox 清單、`.selection-card`、`.radio-list`。（附帶一提，不算五維度發現：頁面引入了 `control-row.css`／`radio-card.css` 但實際沒用到對應 class，屬 dead import，非本次稽核範圍。）

### settings.html
- **`ds-components/settings.css:64`（`.settings-row { border-bottom: 1px solid var(--border) }`）— 元件層預設值錯誤，settings.html 是最大宗受害頁**：整頁大量把 `.settings-row` 當堆疊清單用（同段落內連續多列，只有最後一列手動 `border-bottom:0`；例：`:305-308` Public profile / Show earnings / Show fan count / Allow direct messages 最典型），但元件本體給的是列線該有的 `--border-soft` 卻寫死 `--border`。這不是頁面自寫覆蓋，是元件層預設值本身的問題；同一元件也被 `store-settings.css`／`source-status.css`／`tier-overview.css` 消費（這些頁不在本批次 22 頁範圍內）。級別：**需人判斷**（改法本身機械：`--border`→`--border-soft`；但施工前要照專案「改共用元件要同步所有使用頁」規則，核對範圍外的消費頁，尤其 `store-settings.html` 的 `.ss-stack .settings-row:first/last-of-type` 覆寫是否受影響）。
- `:319,328,338` 的 `background:var(--muted)` 是 34×34 裝置圖示小徽章（icon badge），非分組填色層，不算違規。
- 主題挑選器（`[data-theme-set]`）與導覽模式選擇器（`:139-147` `data-nav-set`）都核對過是排他式單選，Q74 已定案樣式，未誤標。
- `notif-matrix`（`:179-256`）是 MS-D 語意（每格獨立開關），非多選家族取代對象，未誤標；且 `ds-components/notification-matrix.css` 的列分隔線（`:67,89-90`）已經正確用 `--border-soft`，合規。

**批次 D 小計**：明確可改 1 處（project-detail.html:713-715 漏加 `card--muted`）＋需人判斷 4 處（register-ip #ri-usage 元件誤用、register-ip territory chip 對比、project-detail tag-suggest chip 對比、settings.css 列線色跨頁修正）。

---

## 統計（22 頁全數完成）

**明確可改：6 處**
1. `ds-components/data-list.css:36`（＋`:54`）列線色誤用 `--border`，該 `--border-soft`（命中 admin-video-review、auction-detail、bundle-detail、create-bundle、create-project、creator-detail、manage-ip、order-detail、pickup-detail、project-detail、register-ip 共 11 頁）
2. `ds-components/table.css:60` 同上問題（命中 admin-platform-fees、admin-video-review、pickup-detail、project-detail）
3. `create-product.html:393` 過時註解「向上陰影」，`.nest` 已在 2026-08-25 拿掉陰影改上緣髮絲線，`--shadow-nest-up` token 已不存在
4. `register-ip.html:208-217` Permitted usage 用單選家族 `.selection-card` 承載「可複選、至少選一」語意——元件家族選錯，判級錯誤本身明確
5. `project-detail.html:713-715` 交付摘要 KPI 卡漏加 `card--muted`，設計意圖與實作對不上
6. （見下方需人判斷 #9）`ds-components/settings.css:64` 列線色誤用 `--border`——改法機械，但因影響範圍跨出本次 22 頁而歸類需人判斷，此處僅列存在性

**需人判斷：9 處**
1. `create-auction.html:323-328` 建議標籤 chip／`--input-surface`＝`--card` 淺色模式邊界消失
2. `create-campaign.html:44,222-229` `.fc-calc` 用 `--muted` 絕對暗色填色（是否算巢狀分組情境待裁決）
3. `create-event.html:449,5026-5027` lineup chips 淺色模式融邊
4. `create-event.html:4606-4608` 加購商品 chips 淺色模式融邊
5. `create-product.html:738-742` tag-suggest chips 淺色模式融邊
6. `create-project.html:215-217` 格式 checkbox（3 選項）是否升級 MS-B 待裁決
7. `create-project.html:406-409` 廣告版位 checkbox（4 選項）是否升級 MS-B 待裁決
8. `create-project.html:199,233` 題材 chip（21 顆）未選態淺色模式對比不足
9. `ds-components/settings.css:64` 列線色跨頁修正（命中 settings.html 全頁＋ manage-ip.html，且需核對範圍外的 store-settings.html／source-status.css／tier-overview.css 消費頁）
10. `order-detail.html:573-576` 退款品項核取框方向（左置 vs 定案的靠右對齊）與 MS-A 落地順序
11. `register-ip.html:225-234` territory chip 淺色模式融邊
12. `project-detail.html:528-533` tag-suggest chip 淺色模式融邊

（註：上方明確可改 #6 與需人判斷 #9 為同一發現，僅計入需人判斷欄位一次，避免重複計數——最終統計以本段文字敘述為準。）

**根因集中度**：需人判斷清單裡有 7 處（#1、#3、#4、#5、#8、#11、#12）同一根因——`_tokens.css:62` 淺色模式 `--input-surface` 等於 `--card`、疊上剛上線的 chip 無框化。建議優先處理這個 token 級修法（讓淺色模式 `--input-surface` 與 `--card` 拉出一階色差，比照深色模式已有的做法），一次解掉多數 Dimension C 發現。

**其餘 11 頁合規、無發現（除上述跨頁元件層問題外）**：admin-ip-bank-entry、admin-platform-fees、admin-video-review、auction-detail、bundle-detail、create-bundle、create-event-legacy、creator-detail、pickup-detail、product-detail、series-detail、e-shop。
