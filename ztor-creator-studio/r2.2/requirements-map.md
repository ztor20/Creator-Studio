# Ztor Creator Studio · R 2.1 · Requirements Map

| 上游規格 | 頁面 | 覆蓋內容 |
|---|---|---|
| `5.1.10-登入.md` | `login.html`、`ds-components/auth.css`、`js/i18n.js`、`js/icons.js`、`js/sidebar.js`、`feature-scope-map.md` | **新頁（2026-08-04，D170）**。未登入層的統一入口，不掛工作台導航（骨架照 `scanner.html`，不載 `sidebar.js`）。**無卡片版面**（2026-08-04 使用者裁示，內容直接置中在 `--background` 上，比照 ztor 消費端登入頁）。四個畫面住同一個內容欄互切：F1 方式選擇（**六個並列入口一律同款卡**，重用 `.radio-cards--gate` 的 2 欄 grid）→ F2 電子郵件登入（信箱＋可切顯示隱藏的密碼、欄位填妥前主要行動停用、底下忘記密碼）／F3 手機號碼登入（國碼下拉預設 TW +886＋號碼＋驗證碼＋取得驗證碼倒數）→ F6 忘記密碼（只承諾入口，重設流程未定義）。F4 落地分流：Admin → `creators.html`、Artist → `index.html`。F5：電子郵件憑證錯誤顯示不分辨帳號或密碼的一般性錯誤（防帳號列舉）、手機查無號碼顯示「此號碼尚未開通，請聯繫平台」且**頁面上沒有任何通往註冊或建立帳號的路徑**、送出中主要行動改處理中並停用。明確不做：推薦碼／兌換碼、同意條款勾選、自助註冊。**⚠ 第三方登入已加（2026-08-04 使用者裁示，產品範圍變更、規格同步中）**：Apple／Google／Facebook／LINE，單色品牌標記（`icons.js` 的 `brand-*`，Simple Icons CC0，吃 `currentColor`）。**同日第三輪裁示「email phone icon 都換實心的」**：前兩張卡的圖示改用 `mail-fill`／`smartphone-fill`（Tabler filled，MIT，`<g>` 放大 1.14／1.2 倍對齊品牌標記的光學大小），六顆同一量級；這是**限 F1 這一格**的例外，站上通用圖示維持描邊，既有 `mail`／`smartphone` 路徑未動（STYLE-DECISIONS Q51）。**同日第二輪裁示「將第三方全部都用這種格式」**：原本一列四顆只有圖示的方塊（`.auth-oauth`，已移除）改成與電子郵件／手機號碼同款的卡，六張同住一個 2 欄 grid（2 欄 × 3 列，第 1 列兩個帳號方式、第 2-3 列四個第三方）；六張都是「標題＋一行副標」所以等高，四張第三方共用同一句副標「不需另設密碼」（同一個 key `login.method.oauth.sub`；副標一律回答「怎麼驗證」，四句不同會變成把品牌名再講一次）；可讀名稱＝卡面標題。規格 5.1.10 與 D170 目前仍寫「第三方登入不做」，原型先落地、`documents/` 由上游同步；原型不接真的 OAuth，點了直接落地 `index.html`（demo 行為，見 ASSUMPTIONS UIA-105）。**F6 註冊入口在原型中完全不存在**（2026-08-04 使用者裁示整段移除，不再掛 `data-feat="full"`；規格只要求 phase 1–3 不出現，原型更嚴格，見 ASSUMPTIONS UIA-105）。**版面（2026-08-04 使用者裁示）**：內容的垂直起點改成固定（`align-items:flex-start` ＋ 168px 上內距，手機 96px），四個畫面切換時標題不跳（實測四畫面同為 171.6px）。元件 `auth.css`（§4.113 Auth shell）、icon `mail-fill`／`smartphone-fill` ＋ `brand-apple`／`brand-google`／`brand-facebook`／`brand-line`、39 個 `login.*` i18n key（中英齊全；2026-08-04 隨註冊入口移除 2 個、隨第三方登入新增 5 個，同日第二輪把 5 個 `login.oauth.*` 換成 `login.method.group` ＋四個品牌卡面標題＋四張共用的 `login.method.oauth.sub`，共 39 個）。全站帳戶選單的「登出」（`js/sidebar.js` 三個入口的 `[data-logout]`）導回本頁——登出行為上游無規格，見 ASSUMPTIONS 產品缺口 PG-032。demo 手法（身分判定、憑證正確與否、號碼比對、驗證碼倒數、忘記密碼回覆）與待上游項目見 ASSUMPTIONS UIA-105 |
| `5.1.0-Creator管理.md` | `creators.html` | Admin Creator Studio 左側欄的 Creator 管理目的地與進入 Artist Studio 入口 |
| `5.1.0.1-Admin-IP-Bank.md` | `admin-ip-bank.html` | Admin Creator Studio → IP Bank 的導航脈絡、Film × IP × Owner 列表、Linked／Pending、Share、Edit／Remove 與建立入口 |
| `5.1.0.1.1-建立IP-Entry流程.md` | `admin-ip-bank-entry.html` | Film、IP type、Owner username／email lookup、Linked／Pending email invitation、Share 0.01 與 Film ≤100% 驗證 |
| `5.1.0.2-IP-Bank-Reporting.md` | `ip-bank-reporting.html` | Admin Creator Studio → IP Bank Reporting 的導航脈絡、Film 與日期範圍篩選、KPI、Owner／Platform 分潤列與 Pending 鎖定呈現 |
| `5.1.0.3-平台費率設定.md` | `admin-platform-fees.html`、`partials/fee-exception-modal.js`、`js/sidebar.js`、`js/i18n.js`、`js/icons.js` | **新頁（2026-07-16 BR-NEW-4／D139，D141 升級費率樹）**。Admin 第四同層目的地（`sidebar.js` ADMIN_ROUTES/NAV/FULL_ROUTES 三處、icon `percent`）。**三分頁 IA**（`tabs` 元件）：<br>**費率設定**＝支付手續費置頂輕量單列（全站單一、可編輯；**% ＋每筆固定 HK$**，`amount-field--readonly` 前綴，示意 3.4%＋HK$2.40）＋**F2 預設費率樹**（`ztor-accordion` 四維度 E-Shop/Events/Projects/IP × 交易子類葉節點，`ztor-table`＋`amount-field--suffix` %，示意 5/0，IP 維度 pending 保留 15/10）＋**F4 費率版本與生效範圍**（目前版本 badge 獨立一行＋生效日＋Save）；<br>**版本歷史**＝F4 append-only 表「版本/生效/範圍〔預設費率／例外·Creator〕/調整」、不回溯；<br>**費率例外（F3）**＝清單（Creator＋覆寫摘要＋編輯/移除）＋「＋新增例外」。新增/編輯走**彈窗**（partial `fee-exception-modal.js`，復用 `payout-modal` 殼）：新增含 Creator 搜尋（依名稱過濾、排除已有例外者）、編輯固定 Creator；彈窗內平台費樹逐葉覆寫（空=繼承 placeholder、輸入翻「繼承/覆寫」badge、有覆寫維度自動展開）；**支付手續費唯讀顯示**（全站統一、不逐 Creator＝D141）。命名去 General（Creator 保留英文＝全站慣例）。§7.6 平台費率唯一設定入口、僅 Admin 可改，逐 creator 覆寫以葉節點覆寫預設。費率數值、taxonomy、per-IP/活動 覆寫、凍結/對帳引擎待上游/工程（UIA-054）。收益拆解 E22 於 earnings 已建（`data-feat="E22"`）、本輪只驗收。**2026-07-22 對齊確認費率（A，UI-CHANGES 同日）**：支付手續費補每筆固定 HK$ 欄（設定卡＋例外彈窗唯讀鏡射）；預設費率初始值改 5/0（未點名葉節點繼承上一層，IP pending 保留）；版本徽章改延後切換（生效日在未來時維持現行、副行 `#fee-next-note` 標待生效、歷史列標待生效）；移除例外也寫入版本歷史（可稽核）。IP bank fee 上游 pending 未動。**2026-07-22 儲存改分割下拉（B，Figma 865-100）**：儲存移到分頁列右端、改分割下拉主按鈕（立即生效＝今天發版／延遲生效＝開 `#fee-delay-modal` 選生效日發版）；撤除頁面「費率版本與生效範圍」卡與目前版本徽章，版本資訊改由版本歷史分頁承載（仍是 F4 同規則的呈現取捨）|

本文件只對照「上游需求覆蓋到哪個 UI 實作」，不複製或改寫產品規則。

## 權威來源

1. [`../../requirement/02-PRD-產品需求規格書.md`](../../requirement/02-PRD-產品需求規格書.md)
2. [`../../documents/decisions.md`](../../documents/decisions.md) 的有效產品決策
3. [`../../documents/0-設計規格書.md`](../../documents/0-設計規格書.md) 與相關 `5.1.x` 子頁
4. [`BUILD-SPEC.md`](BUILD-SPEC.md) 的呈現與工程決策

發現上游缺口或實作衝突時記入 [`ASSUMPTIONS.md`](ASSUMPTIONS.md)。不得因畫面已存在就把行為寫回上游。

> **2026-07-15 呈現追蹤**：建立流程統一採 `.wizard__sheet--sectioned`（`--surface-page` 底）與 `.form-section--outlined`（亮暗皆 `--card` 填色；dark 原 `--muted`、2026-07-17 midnight Q15 改）；可見區塊以 `--sp-24` 間隔，跨越 `[hidden]` 條件區塊不留空白。採用頁為 create-product／-auction／-bundle／-event／-project／register-ip／admin-ip-bank-entry，`section-test.html` 保留作視覺驗證；不涉及上游需求、頁面責任或產品行為變更。

> **2026-07-14 呈現追蹤**：版本切割由 `feature-scope-map.md` 管理；未列功能只在 Phase 4 呈現，並不表示上游最終產品範圍被移除。

> **2026-07-26 呈現追蹤**：`e-shop.html` 的 Nick persona「祝你好命」選物四件組，將既有同名組合主圖替換為可填滿方形縮圖外框的 1:1 四件商品深色平鋪視覺，並提高背景對比讓黑色衣物材質可讀。此變更只處理素材呈現；組合成員、價格、庫存、上架狀態與 E-Shop 互動均未改動。

> **2026-07-27 資料同步**：Nick persona 的 Products 分頁改以 `wishyouagoodlife.com/products` 及 13 個商品子頁為來源，顯示來源名稱、NT$ 價格、繁中摘要、規格、庫存與本地商品圖；詳情頁沿用同一資料並載入多圖與原商品頁連結。Default／userB 資料集不變；來源頁未提供可驗證成本，成本欄維持空值。

> **2026-07-27 組合詳情同步**：Nick 的「祝你好命」四件組由 E-Shop 帶入穩定 bundle id，`bundle-detail.html` 讀同一資料源呈現組合主圖、四件成員與各自商品入口。來源沒有老帽、褲子、球鞋，三者以組合包延伸視覺單品補入 Products 與詳情附圖，價格標為待確認，不冒充來源網站資料。

> **2026-07-27 拍賣同步**：Nick 的 Auctions 新增 WYAGL／祝你好命客製鞋列與 `auction-detail.html?id=wyagl-nike-dunk` 詳情入口，沿用素材目錄內四張鞋款圖片；不改 Default／userB 拍賣資料。

> **2026-07-31 呈現追蹤**：`create-campaign.html`（無獨立上游規格列、於 5.1.2.2 Project Detail 列內以 `fc.*` 命名空間提及）的圖片上傳槽同步改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。

> **2026-08-02 呈現追蹤（跨 10 頁，無獨立上游規格列）**：全部 10 個詳情頁（`auction-` / `brand-campaign-` / `bundle-` / `event-` / `fan-` / `ip-` / `order-` / `pickup-` / `product-` / `project-detail.html`）的麵包屑最前面加返回上一層按鈕（`.page-crumb__back`），href 固定指麵包屑的上一層。**只在整頁滿版那種開法出現**——從清單點進來的 `detail-sheet` 覆蓋層（`?embed=1`）本來就有自己的「← 回到 XXX」，該情境下由 `html[data-embed]` 隱藏。純呈現補位，不改任何導航目的地或產品行為（見 UI-CHANGES 同日條目、design-system.md §6.3）。

## 覆蓋對照

> 2026-07-24 B 反饋：項目詳情設定分頁的「發布更新」「＋ 新增合作者」與「前往我的 IP 連結」統一為滿寬置中的 outline 入口；新增合作者改放其卡片底部。僅改呈現，不改既有 modal／連結行為。

| 上游規格 | R 2.1 實作 | 狀態 |
|---|---|---|
| 5.1.0 Creator Management | `creators.html` | 新增（D086）：Admin Tier 0 名冊＋建立 creator。`sidebar.js` 渲染 Admin 導航——roster 鎖 Tier 1／creator scope logo 前返回＋「管理中」標示；creator 切換走 devtools cheat code。範圍＝完整現有工作區。**2026-06-30（D097）**：狀態枚舉定案＝啟用中／已停用（兩值）；建立改 popup、停用／啟用（⋯ 選單）、搜尋／篩選、整列可點。**2026-06-30（D098→D099）**：F 項依電子商店分類重編號，最終＝**F1 頁首／F2 名冊工作列（狀態篩選＋搜尋同列＋建立）／F3 Creator 名冊（列＋列操作 前往/停用/啟用，含欄位表頭）／F4 進入與返回**；搜尋/篩選/列改吃 e-shop 同款 field-pill／filter-tabs／product-list、管理鈕改 Enter（前往）。D099 移除 D098 一度加入的名冊概覽（bento），各狀態數量由 filter-tabs count 承載。建立必填／店鋪唯一性／代操稽核／已停用可否代操仍待上游（ASSUMPTIONS UIA-029..032、036）。**2026-07-03（D107）**：(a) 未選 creator 時導航**只留「Creator 管理」marker**、移除鎖住的 Tier 1 模組列（`sidebar.js` locked→空字串，撤 UIA-030「roster 鎖 Tier 1」呈現）；(b) 建立表單擴充＝頭像＋email（必填）＋電話（選填）＋店鋪網址（`ztor.com/shop/…` 即時預覽、handle 平台唯一→擋重複）；(c) 名冊列加「創建時間」欄（`CREATORS` 補 `email/phone/created`）。皆前端 demo（UIA-045）；操作紀錄檢視 UI 未建（PG-015）。**2026-07-22（B 反饋，對齊 BR-02）**：建立 creator 由單張憑空表單改為**搜尋帳號＋就地建檔的單面板**（同日幾輪反饋收斂：曾做兩步精靈＋stepper，最終去 stepper、不分步）——搜尋續用 IP Bank Entry「權利人」同款 `owner-lookup` 即時 typeahead，依人名／username／email 搜前台已註冊、尚未建檔的帳號池（`sidebar.js` 新增 `REGISTERED` demo 6 筆＋`window.ztorCreator.registered`），下拉挑一筆＝選定（無 IP entry 版的 email invite 路徑，只借 `owner-lookup.css` 視覺）；選定後下方就地出現建檔欄位（逐列一欄一列）：放大頭像＋名稱／Email 唯讀、Admin 只設店鋪網址（唯一擋重複）＋電話；建立＝onboard 進名冊並自池移除。對齊 §5.1.0 頁面定位 BR-02「creator 來源＝前台已註冊帳號」；名稱·Email 帶入唯讀屬 D107 建立欄位的呈現詮釋（ASSUMPTIONS UIA-071，規格 F2 建立欄位宜回頭對齊）|
| 5.1.0.4 影片上架審核 | `admin-video-review.html`、`js/work-review-store.js`、`ds-components/review-status.css`、`js/sidebar.js`、`js/i18n.js` | **新頁（2026-08-07，D179）**。Admin 的全平台影片作品送審處理，與 Creator 管理／Admin IP Bank／IP Bank Reporting／平台費率設定同層（`sidebar.js` 的 `ADMIN_NAV` 由四項增為五項，對齊 §3.2 產品地圖 Tier 0），不需選定 Artist。一頁兩種檢視、由 `?id=` 決定：**F2 待審清單**（每列＝一件送審作品：封面／標題／創作者／來源項目連回專案詳情／作品審核狀態／送出時間／等待時間／送出次數／審核者；預設排序＝待處理排最前、同狀態內等最久的排最前；狀態頁籤附件數；依標題·創作者·來源項目關鍵字搜尋；無送審件與查無符合兩種空狀態各有各的說法與清除入口）→ **F3 送審內容檢視**（照作品上架流程四個步驟原序逐項呈現全部欄位，欄位定義以 5.1.2.2.1 為權威、本頁不重抄；來源項目脈絡含項目狀態與待發貼文的內容與受眾；送審歷程 append-only、含每次送出·接手·裁決的時間·執行者·退件理由；唯讀邊界寫在頁尾說明）→ **F4 審核動作**（待審核先接手審核轉審核中並記下審核者；審核中才給審核通過與審核不通過，兩者各過一次確認，退件理由必填；已審完只保留檢視）→ **F5 審核狀態與創作者端呈現**（狀態枚舉照主規格 §7.2 作品審核列，由 `js/work-review-store.js` 提供給三個消費頁；創作者端落點在 5.1.2.2 §2.2.9）→ **F6 權限與紀錄**（僅平台營運可進入；無權限狀態顯示需要的身分而非隱藏入口）。**審核通過一次完成三件事**：作品上架、完成作品更新依所選受眾發布、項目 Succeeded → Live——三件事在創作者端由 `project-detail.html` 播出（讀 store 的 `pendingRelease()`，播完標記 released 不重播）。呈現決策：新元件 `review-status`（§4.119，與創作者端共用）、清單沿用 `list-toolbar`＋`tabs`＋`admin-table-wrap`／`ztor-table`、送審欄位用 `kv-list`、歷程用 `data-list`、兩個確認彈窗用 `payout-modal`。呈現假設見 ASSUMPTIONS UIA-111。 **2026-08-07（第三輪）F2／F3 的「來源項目」連結帶創作者身分**：審核佇列是全平台的，而項目 demo 資料逐創作者一份，所以每一筆送審件多存 `persona`，連結改為 `project-detail.html?id=…&creator=…&from=admin-video-review.html`——依 §4.1「Admin 選定並進入某 Artist 工作區」的模型，身分跟著連結走；落地後由 `js/sidebar.js` 顯示既有的 Admin 代管標示與返回審核佇列的入口（未新增第二套標示）。來源項目脈絡的「項目狀態」改用指名資料集的查詢（`ztorProjects.getIn`），查不到顯示「—」而非一律「已成立」。persona 是原型的 demo 機制、不是產品欄位，見 ASSUMPTIONS UIA-112。|
| 5.1.1 Dashboard | `index.html` | F1–F8 已覆蓋（五列佈局）。2026-06-14 對齊規格新約束：F3 近期收入只列已結算收入、移除狀態欄（`hideStatus`）；F2 總收入環比改週粒度＋顯示最後更新＋深連結 Earnings；F4 補 Snoozed（軟關、排除 F2 計數）與阻斷型 disabled 關閉。F2 待處理／F4 「View all」缺「完整待辦視圖」頁、F5 缺「完整動態視圖」頁（PG-006）。**2026-08-06（B 反饋）**：bento 之下新增「ztor 粉絲組成」圓餅卡（自 5.1.7.8 搬來，使用者裁示），分母只算站內有 ztor 帳號的粉絲、與 F7 受眾數字不同軸，卡頭膠囊與卡底註腳寫明；純呈現搬移，不新增資料欄位或計算。**2026-08-07（B 反饋）**：使用者裁示「ztor 粉絲組成要放在一進入 dashboard 的畫面中」，該卡由頁尾移到 F2 之下、F3｜F4 之上——正好回到規格 §頁面佈局 給 F9 的第 3 順位，先前放頁尾是實作漂移，本輪一併對齊。其餘區塊順序與 span 全部未動，項目一個不減。**同日第二段**：使用者指出該卡獨佔整列太寬，要從頁面上挑一個更有意義的區塊上來併排——挑 F4 今日待處理（行動價值最高，且是 F2「待處理事項」計數的明細，數字與明細同屏才閉環）。新佈局＝F2 → **F9 `span-7`｜F4 `span-5`** → F3（落單，改滿寬，順帶解掉作品名截斷）→ F5｜F6 → 平台表現 → F7｜F8。項目仍一個不減，只有配對關係與 F3 寬度改變；規格 §頁面佈局 第 4 列原本的「F3｜F4」並排關係因此變動，屬呈現層安排（該段明言欄數欄寬由 project-ui-creator 決定）。新增 `.bento--top`／`.bento--stack-md` 兩個元件修飾子支撐這排。**同日第三段**（使用者：「這樣空一大塊不行」）：F7 由頁尾疊到 F9 底下同一欄（新元件 `.bento__stack`），左欄 828px 與右欄 F4 783px 收齊、空白消失；F8 遞補到 F3 旁邊，原 F7｜F8 整排消失。**最終佈局＝F2 → [F9＋F7]`span-7`｜F4 `span-5` → F3 `span-7`｜F8 `span-5` → F5｜F6 → 平台表現**。九個區塊一個沒少，改的只有配對；整頁 3,734px（原 3,851px）。|
| 5.1.2 Projects | `projects.html` | 已有清單與建立入口。**2026-07-24（B 反饋）**：清單的募資／預購目標沿用既有 `p.bar.pct`，依序顯示小一級百分比、可重用 6px `.project-bar`、已募／目標金額三排；目標欄由 128px／120px 微增為 144px／136px，桌機欄間距由 16px 微增為 20px，目標格左右各加 4px 內距，非 campaign 列維持「—」。類型由獨立欄移至項目名稱上方。純呈現更新，不新增計算、資料欄位或狀態規則。**2026-08-06（B 反饋）**：狀態 `published` 的對外字樣由「共創中／Co-creating」改中立的「進行中／In progress」（篩選頁籤、列徽章、項目詳情徽章三處同步），並移除詳情頁「依發行模式分流字樣」的補丁；狀態集合與狀態機未變，規格 §7.2 仍把各模式對外字樣列為〔產品待確認〕（ASSUMPTIONS UIA-104）。 |
| 5.1.2.1 Create Project | `create-project.html` | 已實作；需複查模式規則與費率引用。**2026-07-24（D150，B 反饋／UIA-086）**：流程前置「項目類型閘門」——分步表單前先出獨立畫面（三列 `.radio-list--menu` 型別選項＋返回鈕），選定後才進表單；表單內「項目類型」同步改同款 compact 圖示列。兩處共用 `chooseType` 同步、型別仍驅動 FLOWS；選中提示為列底色＋右側橘點，不用橘色外框。step-1 標題改 `cpp.about.h1/sub`；補掛 radio-list.css。呈現重組、產品規則未改。**2026-07-30**：「回饋套組」步驟的編輯器抽出為共用模組 `js/bundle-editor.js`（`window.ZtorBundleEditor.mount()`），與 project-detail「方案與承諾」共用同一支；本頁只保留名額池讀 `#fd-slots`、右軌名額分帳、Continue 擋關三件事，元件指向 `ds-components/bundle-editor.css` ＋ `js/bundle-editor.js`。**2026-07-30（第二批，使用者裁決）**：套組的「價格」欄位由創作者輸入改為推導欄位（唯讀，名額×每名額單價＋商品定價加總），新增「套組優惠」欄位（僅能折抵商品那一段，地板＝股份總價）；本頁新增每名額單價來源 `#fd-perslot`（募資步驟既有唯讀欄位，目標金額÷支持者名額）餵給 `mount({ getPerSlot })`。**2026-07-30（第三批）**：套組優惠欄位單位由金額改為百分比（0–100%）；套組卡展開態欄位順序改為「名稱→一句話說明→含分潤名額｜販售上限→商店商品→額外權益→定價區塊」（定價區塊置卡片最後）。**2026-07-30（第四批，B 反饋）**：「發布項目」的落地狀態由 alert 佔位改為已導向詳情頁＋成功提示——`publish()` 改把成功訊息排進 `js/toast.js` 跨頁佇列，導向 `project-detail.html?id=<與所選類型相符的示範專案 id>`（原型替身，見 ASSUMPTIONS UIA-088）；排程發布改用「準備中」語氣文案（2026-08-04 依 D151／v3.10 現行狀態術語更正，原「已排程」為舊詞殘留，見 UI-CHANGES 同日條目）。Wizard footer 新增擋關原因行 `.wizard__bottom-hint`，回饋套組步驟 Continue 被停用時顯示原因。**2026-07-30（第五批）**：套組卡展開態的價格移至標題列右上角（`.fc-sum__tag`＋放大 `.fc-sum__val`），動作改移至卡片底部（整條可點的收合把手 `.fc-collapse`＋紅色移除鈕 `.fc-bundle__foot`），卡內間距對齊建立商品頁 `.form-section--outlined` 的節奏（`--sp-24`）。**2026-07-31 呈現追蹤**：圖片上傳槽改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。**2026-07-31（同日第二輪，依 D164／§7.10）**：F6 圖片素材槽由縮圖／直式海報／橫式橫幅／圖庫 4 格併為**主視覺＋圖庫** 2 格，兩者皆必填；PG-023 已結案。**2026-08-03（D166，規格 §5.3.2 F28／F29、§4.3 F12 第 4 條）**：預購由 4 步改 5 步（About→Showcase→Presale→**Tiers（新增）**→Review），退場「預購內容」自由文字欄位（原 `cpp.po.included` 系列 i18n key 已刪，其職責由新的預購方案步驟承接）；新步驟 `data-panel="tiers-pre"` 掛載共用模組 `js/bundle-editor.js` 的 `shares:false` 模式（同支模組、關掉分潤名額，見 §5.3.2 說明與 `design-system.md` Bundle editor 條目），對應 §4.3 F12 第 4 條「Presale 至少 1 個方案已建立」的發布前檢查、Review 摘要補方案數。該步驟無右軌（見 STYLE-DECISIONS Q44，與共創的 funding／tiers 兩步皆有 `wizard-split` 右軌不同，因為預購沒有名額分帳或目標覆蓋率可算）。 **2026-08-10（直接發佈與作品上架整併，規劃書方案 B）直接發佈依作品家族分流**：影片家族（電影／短劇／影集／MV／文檔）走五步 `golive-film`（關於作品 → 作品檔案 → 展示內容 → 詳情與定價 → 送審確認），非影片家族維持現行四步、發布即上線；分流點在步驟一選完內容類型的當下、stepper 在那一刻才定稿。新增的三個步驟內容全部來自 5.1.2.2.1 的 F1–F15，由共用元件 `partials/work-fields.js` 渲染（與 `publish-work.html` 同一份實作，見該列）。重疊欄位只問一次：主視覺＝封面、圖庫＝劇照、宣傳影片併入預告片、存取模式併入是否付費、單一價格併入逐畫質租賃、發布時間併入上映日期；F4 類型欄位的導演／片長／分級目標／演員陣容／類型 Genre／語言在這條路徑退場（`data-gl-drop`），項目名稱與描述改由逐語言作品文案承接（`data-gl-hide`）。廣告（F16）與品牌合作（F17）兩張卡是同一個 DOM 節點在兩條路徑之間搬移（`[data-shared-adsbrand]`），不複製第二份。送出＝送審：寫進 `js/work-review-store.js`（項目進準備中、作品進待審核），Admin 影片上架審核頁看得到；送審確認那一步的「接下來會發生什麼」為送審版文案（規劃書第 13 題）。整併前的四步版留在 `golive-4step/create-project-4step.html`，由 cheat code 測試版 `r2.2_golive-4step` 改接（凍結、不再維護）。呈現假設與待裁決見 ASSUMPTIONS GLF-001。 **2026-08-10（第二輪，D182 第 7／8 題＋主規格 §7.11／§7.1.2）F4 兩項值域收斂**：分級目標（自訂值域、選項待補）改名年齡分級並改吃 §7.11 六級單選（`tax.age.label` ＋ `pw.age.*`）；電影／短劇／影集組與 MV／文檔組的類型（Genre）自由文字改名分類、改吃 §7.1.2 題材軸 21 值封閉多選（`pw.info.genre.title` ＋ `pw.genre.*`，`chip-group` 多選）。**音樂／音樂專輯、活動、自訂三組維持自由文字**（§7.1.2 使用限制第 4 條：那三組的題材值域上游未定）。兩份值域住在新元件 `js/work-taxonomy.js`，與作品上架的 F11／F13 同源（見 5.1.2.2.1 該列）；本頁只留空殼 `data-taxonomy-age`／`data-taxonomy-genres`。這兩格在直接發佈影片路徑仍隨 `data-gl-drop` 退場、由第四步的作品欄位承接。 |
| 5.1.2.2 Project Detail | `project-detail.html` | 2026-06-15 新建：Overview/Content/Public details/Money 四分頁，含狀態時間軸、合作者分潤、收益瀑布、淨利池手動分配、募資監看、NFT 治理唯讀；金額引用 Earnings §7.3 不重算。由 projects.html 列進入。多項目資料綁定與狀態轉換確認流程待後續。2026-07-24 版型改版：新增 `.page-intro` 頁首承接標題與全部操作（原 hero 右側 Actions rail 撤除），hero 改 `.ip-hero--project` 兩欄＋新元件 `.funding-panel`，比照公開端共創計畫詳情頁。同日四個分頁重新分類為 項目總覽／方案與承諾／關於項目／項目收益（「狀態」卡改名項目歷程），五個編輯彈窗由 drawer 改中央彈窗（STYLE-DECISIONS Q27）。同日移除收益頁的「影視／音樂」segmented，項目內容類型改由清單點進來的項目決定：新增共用資料層 `js/projects-store.js`（清單與詳情頁同源），詳情頁讀 `?id=` 渲染名稱／封面／徽章／募資數字並依內容類型家族決定音樂版稅區塊顯隱；demo 項目補到 11 筆、覆蓋 5.1.2.1 §4.1 F3 的十種內容類型（UIA-083）（依「這件事在做什麼」分，非資料型態），支持方案改為 inline 多套組 editor；發布後可編輯範圍待上游裁決（PG-018）。2026-07-24 再調整：hero 改 GoFundMe wireframe（.pd-hero）、移除重複的收益概況、新增「製作進度」tab（整合項目時間與里程碑＋作品進度）、展示內容改建立流程同款上傳格。**同日版稅改獨立 tab，僅 `fund + published + music` 顯示；`adia-chan` 是 UIA-084 的唯一驗收示意樣本，CCR-006 資料／頻率／分帳規則未定。** **同日 B 反饋：操作鈕移入 `.pd-detail__topbar`，與 breadcrumb 桌面同列、窄版自然換行；hero header 只保留標題。** **同日 B 反饋：設定分頁的發布更新與合作者均重用 `.ztor-table`，各自有明確表頭與欄位；合作者擴為全寬。發布更新入口移至區塊下方的 primary 按鈕，項目完成改由發布 popup checkbox 選擇，均屬既有 demo 行為的呈現重組，未改產品規則。IP Rental 未綁定提示橫幅依反饋移除；保留前往 `my-ip.html` 的主要按鈕，文案為「前往我的 IP 連結」。** **同日 B 反饋：所有保留的項目編輯 popup 改用既有 `.field` 與 `.form-grid`；支持方案重組為使用 `.form-section--outlined`、`.picker` 的頁內多套組 editor，保留方案名稱、價格、E-Shop 商品引用、名額／不限量、額外權益與最低三組規則。商品引用只可從既有 E-Shop picker 選擇，新增／移除套組與權益均為不持久化的本頁 demo，未新增計算、發布後限制或產品規則。** **2026-07-26 B 反饋／撤除：展示內容相簿移除兩張與項目無關的舊假圖（僅留動態封面格）；我的收益「共創計畫．深度明細」兩張卡改滿版（`bento--span-12`）；支持方案套組編輯器更名「組合包」（僅本頁 `pd-bundle.*` 命名空間，create-campaign 的 `fc.*` 副本未動）、卡頭依名稱欄位即時顯示「組合包：{名稱}」、卡頭間距加大；我的收益分頁頂部「探索原型」提示橫幅撤除。均屬呈現層調整，未改資料模型或產品規則。** **2026-07-30**：支持方案編輯器改用與 create-project「回饋套組」步驟共用的模組 `js/bundle-editor.js`（`window.ZtorBundleEditor.mount()`），取代先前的頁內第一代實作；欄位對齊建立頁（套組名稱／價格／一句話說明／含分潤名額／販售上限 自動·限量／商店商品 引用既有 SKU／額外權益），名額池固定 `SLOT_POOL=100`（原型值，真實來源待上游，見 ASSUMPTIONS PG-020）；元件指向 `ds-components/bundle-editor.css` ＋ `js/bundle-editor.js`。**2026-07-30（第二批，使用者裁決）**：套組「價格」欄位由推導取代手填（唯讀，名額×每名額單價＋商品定價加總），新增「套組優惠」欄位（僅能折抵商品那一段，地板＝股份總價，超過上限自動只折抵到上限並提示）；本頁沒有募資設定欄位可讀每名額單價，改由 `fund.goal ÷ 200` 推導名額池、每名額單價固定 $200（`project-detail.html:1675-1678`，原型假資料，見 ASSUMPTIONS PG-020）。**2026-07-30（第三批）**：套組優惠欄位單位由金額改為百分比（0–100%）；套組卡展開態欄位順序改為「名稱→一句話說明→含分潤名額｜販售上限→商店商品→額外權益→定價區塊」（定價區塊置卡片最後）。**2026-07-30（第五批）**：套組卡展開態的價格移至標題列右上角（`.fc-sum__tag`＋放大 `.fc-sum__val`），動作改移至卡片底部（整條可點的收合把手 `.fc-collapse`＋紅色移除鈕 `.fc-bundle__foot`），卡內間距對齊建立商品頁 `.form-section--outlined` 的節奏（`--sp-24`）。**2026-07-31 呈現追蹤**：展示內容的圖片上傳槽改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。**2026-07-31（同日第二輪，依 D164／§7.10）**：§2.2.2 展示內容文案改為「第一張為主視覺、其餘為圖庫」，取代原本列舉縮圖／海報／橫幅／相簿四種角色的敘述；IMG-001 已由上游採納（§7.10），不再是待同步的呈現先行狀態。**2026-08-03（D166）**：分頁顯示條件改依 §2.0 類型對照表——新增「預購方案」分頁（對應 §2.2.5 方案與承諾，預購段）與呼應 §2.2.10 的「製作進度」分頁擴及預購型（原僅共創；`data-type="fund preorder"`，見 §2.0 表「§2.2.10 製作進度｜共創✓／預購✓」）；「支持方案」分頁維持共創專用（`data-type="fund"`）。新分頁掛載 `js/bundle-editor.js` 的 `shares:false` 模式（`#pd-bundle-editor-pre`），2 張種子方案為原型假資料（見 ASSUMPTIONS UIA-103）。共創特有的共創監看（§2.3.3）、淨利池 NFT 分配（§2.3.2）維持只給共創，未擴及預購。  **2026-08-07（A 規格 5.1.2.2 §2.2.9／新規格 5.1.2.2.1／D178）發文類型**：§2.2.9 發布更新彈窗的「此更新同時標記項目完成」checkbox 撤除，改成第一格的「類型」下拉（一般更新／完成作品，預設一般更新）。選「完成作品」時就地換說明行與送出鈕文案，影片家族接 `publish-work.html`（見下一列）、其餘家族因流程未開放而直接發文並轉 Live。前置條件（狀態須為 Succeeded）實作為選項 disabled ＋ 說明。**「已完成（Completed）」狀態退役**：`SAVE.update()` 原本寫死的 `project-detail.status.completed` ＋ `badge--success` 改為走頁內既有 `STATUS`／`STATUS_TONE` 表的 `live`（已上線／`badge--orange`，與 `projects.html` 的 `STATE` 表同源），對齊主規格 §7.2 狀態機；`project-detail.status.*` 七個孤兒 key 一併刪除。 **2026-08-07（第二輪，D179）§2.2.9 補作品審核狀態呈現**：項目更新區段上方新增 `.review-status` 塊——作品審核狀態徽章、最近送出時間、送出次數、審核完成時間、退件理由與「修改後重送」入口（只在審核不通過時出現）。**與頁首的項目狀態徽章刻意分開**（規格明令兩套狀態不得互相代用）。等待中的說明寫明作品未上線、貼文未發出。發文彈窗的「完成作品」在有在途送審件或已通過時停用並就地說明。審核通過時由本頁播出三件事（讀 `js/work-review-store.js` 的 `pendingRelease()`：插入完成作品貼文列、`ztorPdSetStatus('live')`、標記 released 不重播）；因 `projects-store` 是純記憶體資料，載入時先用 store 的 `projectStatus()` 把已通過項目的狀態覆寫回 live，否則重整會退回已成立。 **2026-08-07（第三輪）查無此 id 改顯示錯誤狀態**：帶了 `?id=` 卻查不到時整頁換成 `empty-stub`（沿用查無活動 id 的既有裁決 D154），不再靜默退回清單第一筆——那個退路會讓跨創作者的連結端出別人的項目；沒帶 id 時仍看清單第一筆。落地用哪一位創作者的資料由 `?creator=` 決定（`js/theme.js` 套用後即從網址移除）。 **2026-08-10（D182 第 7 題＋主規格 §7.11）§2.2.8 公開資訊的年齡分級改值域**：原本的 Content Warnings（None／PG／PG-13／R／Adults only，原型實作為 `PG-13／G／PG／R／NC-17`）退場，改吃 §7.11 六級單選，與建立流程 F4、作品上架 F13 是同一份資料（值域來源＝新元件 `js/work-taxonomy.js`）。欄位保留、只換值域與標籤 key（`tax.age.label`）；i18n 刪 `project-detail.details.rating`／`…rating-val`。同輪修掉這一格在唯讀模式整顆消失的問題——zselect 觸發鈕是 `<button>`、被 `shared.css` 的「檢視模式藏掉所有按鈕」規則掃到，改由 `<select data-view-safe>` 傳遞豁免（見 `design-system.md` §4.110）。 **2026-08-11（D182 第 11 題）新增作品區段 §2.2.11**：項目詳情新增「作品」分頁（`data-tab="work"`），承載已送審或已上架的影片作品——封面與標題、作品審核狀態、上映日期、最近送出／送出次數／最近修改，退件時附理由，以及編輯入口（連 `publish-work.html?mode=edit`）。**出現條件＝影片家族 ＋ `js/work-review-store.js` 有這一件紀錄**（直接發佈建完就有、共創與預購送審後才有，對齊 §2.0 類型表）；狀態與 §2.2.9 那一塊讀同一筆紀錄、不各記一套。同輪把「完成作品送審」那一塊對直接發佈項目隱藏（那一塊講的是那則貼文，直接發佈沒有，D182 第 9 題）。元件：`review-status` 新增選配 `__thumb`。送審分界與五項〔產品待確認〕見 ASSUMPTIONS WEDIT-001。 |
| 5.1.2.2.1 作品上架流程 | `publish-work.html`、`ds-components/entry-list.css`、`js/i18n.js` | **新頁（2026-08-07，D178）**。項目成立後把完成的影片作品建檔上架的四步流程，唯一入口是項目詳情 §2.2.9 的「完成作品」發文（不進主導覽）。步驟與欄位照規格 §4：**一 音訊與字幕**（F1 影片檔／F2 原始語音單選／F3 字幕檔可多筆、每筆各標一種語言）→ **二 封面與影音素材**（F4 封面圖單張、比例引用主規格 §7.10 的 `--img-portrait`／F5 劇照多張／F6 花絮／F7 預告片，版面順序與規格同序、花絮在預告片之前）→ **三 影片詳情**（F8 多語言標題與說明，預設繁中與英文兩組、可新增可移除、至少留一組、同一語言不可重複／F9 片長 時分秒／F10 上映日期 年月日／F11 分類 21 項多選、選項照規格去重清單／F12 標籤／F13 年齡限制六選一預設全部／F14 定價：是否付費開關＋幣種 HKD｜TWD＋畫質列可增刪，每個畫質各有租賃價格·租賃爆米花粒·租賃天數·有效天數）→ **四 演職人員詳情**（F15 九個角色各可多筆＋備註）。共用操作照 §4 表單層：儲存為草稿、回上一步、可點步驟指示跳回已完成的步驟、送出只在第四步；**送出前跑 §8 的就緒檢查**（F1／F2／F4／F8 每個語言組／F9／F10／F11／F13，付費時加 F14 每個畫質的四個數值），未通過即停用送出鈕並由 footer 的 readiness chip 逐項列出缺哪一欄、在第幾步。**送出＝送審（2026-08-07 第二輪，D179／§3.3）**：走完第四步把送審內容寫進 `js/work-review-store.js`（作品審核狀態由草稿轉待審核）、queue「已送出審核」提示，回 `project-detail.html?id=`；送出當下作品未上線、完成作品貼文未發出、項目維持已成立。三件事改由審核通過觸發（見 5.1.0.4）。被退件後從項目詳情同一入口回本流程修改重送，送出次數 +1。呈現決策：骨架沿用 `create-project.html` 的 `.wizard__sheet--sectioned`＋`progress-stepper`＋底部 back／next，上傳一律 `upload-tile` demo（`data-upload="content"` 影音、`data-upload` 圖片），畫質、字幕與語言組各用一張 `.card--muted`，新元件 `entry-list`（演職名單），就緒檢查沿用既有 `readiness` 的 footer chip 變體（只在第四步顯示）。**2026-08-07 使用者反饋兩項**：F8 語言組的標題範例文字改成不指名任何作品的通用寫法（沿用建立項目同一格的說法）；F15 九個角色的「再加一位」改成該角色最後一格填了字才出現（元件層 `entry-list[data-last-empty]`，新增與刪除後焦點交給相鄰輸入格）。規格 §5 明示步序與資訊順序以外的呈現形式非約束。**2026-08-07（第二輪）補三處落差**：F1 新增上傳與處理狀態（卡頭唯讀徽章 尚未上傳／上傳中／處理中／已就緒，元件層新增 `upload-tile` 的 `.is-processing`，處理未完成不得送出，對應 §8「檔案就緒」）；F5 劇照由固定四格改可多張（填滿末格自動長出下一格、清空中間格即移出）；F6 花絮由單格改可多筆（一張卡一支，做法同字幕卡）。**未實作的規格項目**：只剩上游自己標〔產品待確認〕的審核營運規則（審核時效、審核者指派、退件次數上限、劇照張數與花絮筆數上限等）——見 ASSUMPTIONS UIA-109。 **2026-08-07（第三輪）**：帶了 `?project=` 卻查不到時同樣顯示查無項目的錯誤狀態、不讓走這一趟（否則會生出掛在不存在項目上的送審件）；未帶參數的獨立開啟行為不變。 **2026-08-10（直接發佈與作品上架整併，規劃書方案 B）本流程成為「作品上架的共用步驟組」、有兩個宿主**：四個步驟的欄位區塊全部抽進純 JS 元件 `partials/work-fields.js`（`window.ZtorWorkFields`），本頁只留空容器與順序（`data-wf`）、步驟切法與送出行為；第二個宿主是 `create-project.html` 的直接發佈影片路徑（見 5.1.2.1 該列）。欄位、就緒檢查（§8）與送審內容三者都住在模組裡——兩頁各算一份，「算不算填好」就會有兩個答案。同輪定價區塊末尾補上平台費揭露（F14 末段「收入與平台費引用 §7.3 與 §7.6 費率樹」），本頁原本沒有這行。四步流程、送出＝送審、退件重送的行為完全未變。 **2026-08-10（第二輪，D182 第 7／8 題）F11／F13 的值域搬進 `js/work-taxonomy.js`**：題材 21 值與年齡六級成為全站單一來源，建立項目流程影視組（5.1.2.1 F4）與項目公開資訊（5.1.2.2 §2.2.8）吃同一份；`partials/work-fields.js` 改成引用、不再自帶副本。本頁欄位、必填與呈現皆未變（中文欄名仍是規格寫的「年齡限制」，`pw.info.age.title`）。 **2026-08-11（D182 第 11 題）新增編輯態 `?mode=edit`**：作品上架後的編輯沿用同一組四步與同一份欄位，`ZtorWorkFields.fill(work,{coverSrc})` 把既有內容寫回欄位；頂部 `info-banner` 隨改動說明這次會不會重新送審，最後一步主動作換字（儲存變更／儲存並重新送審）。改影片檔（F1）／定價（F14）／年齡限制（F13）走 `ztorWorkReview.submit()` 重新送審，其餘照規格分界走新增的 `saveWork()`（不動審核狀態、歷程留一筆 `edit`）。欄位、必填、四步與第一次上架的送出行為皆未變。 |
| 5.1.3 IP Market | `ip-market.html` | 已有探索；2026-06-15 補 F1 搜尋、F2 Availability 三態篩選、F3 素材包完整度（§7.7）、冷啟動／無結果空狀態 |
| 5.1.3.1 IP Detail | `ip-detail.html` | 2026-06-15 交易呈現降級為「權利資訊＋詢問」：估價非扣款、Send＝建 Draft 進核准佇列、競標僅預覽（對齊 §3.3.5／§3.4，PCR-003／PG-010 已解決）。**2026-07-30（使用者回報按鈕無反應）**：「送出租用申請」接上流程——照 §F4 送出後結帳 CTA 整區換成申請狀態（Draft 徽章＋等待權利人核准＋送出摘要＋不扣款說明），滿足 §F3「不得誤以為已完成授權」；附可逆的「撤回申請」。撤回規則、二次確認、承租方追蹤頁與「稍後再說」的去處均待上游（UIA-098）。 |
| 5.1.4 My IP（含 F6 Add your IP 入口） | `my-ip.html` | 部分覆蓋；Add your IP 入口 → register-ip.html（5.1.4.1）。**2026-07-27（使用者裁示）**：清單列整列可點，且目的地由 `ip-detail.html` 改為新頁 `manage-ip.html?id=row<N>`（5.1.4.2）——原本點自己的 IP 會落到 IP 市場的承租方頁、且該頁不吃 `?id=` 故八列全開同一筆示例。列的 kebab「管理」與整列點擊同一個目的地（整列點擊讀該列的管理連結，不寫死）。**2026-07-30（使用者指定）**：自有清單補「肖像權」「聲音」兩列（row9／row10，周湯豪 persona 下為「周湯豪的肖像權」「周湯豪的聲音」），歸站外登錄、沿用 row7 的已驗證樣板；IP 類型走 register-ip 既有的 Person-Based（副標已含 likeness／voice），未新增產品規則。tab 計數與 KPI「IP 總數」同步（8→10、12→14）。IP 類型 enum 兩套並存待上游收斂（UIA-094）。**同日**：兩列依使用者指定移到清單第一、第二位，DOM 順序改為肖像權／聲音 → Ztor 五列 → 其餘站外登錄三列（來源分組由 filter-tabs 承擔，順序不承載語意）。 |
| 5.1.4.2 Manage my IP（新） | `manage-ip.html` | 2026-07-27 新建：擁有者面。四分頁＝總覽（收益／進行中租約／目前費率 KPI ＋ 權利與歸屬）／授權與定價（上架開關 gate 出租條件四欄＋單一儲存）／租賃紀錄（逐筆合約，空狀態有 fallback）／設定（接受新租約·計價模式·到期提醒，自 `ip-detail.html` 的 owner panel 遷入；下方結束上架）。資料源 `js/ip-store.js`（結構欄位）＋ my-ip 既有 i18n `my-ip.row<N>.*`（名稱／權利／租出數／收益／價格，隨 persona 走，兩邊不重複存）。待上游：逐筆租賃欄位集（UIA-090）、有生效租約時定價欄位可否編輯（UIA-091） |
| 5.1.4.1 Register IP | `register-ip.html` | **2026-07-30 送出收尾接上流程**（使用者回報按鈕無反應）：最後一步「登錄 IP」原本只跳 `alert()` 佔位，改為照 §7.1「建立後落點＝IP 資產」導回 `my-ip.html`，成功提示用 `ztorToast.queue()` 跨頁帶過去（`show()` 會在導頁瞬間被丟掉）；文案依上架／私有分兩句，私有那句帶出「之後可從 IP 詳情頁上架」（§4／§6.5）。`my-ip.html` 補載 `toast.js` 才收得到。原型不真的建立 IP record。 **2026-07-30 素材上傳區（UIA-096，產品變更提案）**：第 1 步在名稱／描述之後新增 `#ri-assets`：單一上傳區＋已上傳檔案列表（`data-list`），上傳區下方提示這一型建議上傳什麼（共通封面＋圖集；故事世界／音樂／活動形式 7、品牌 6、其他 3；真人形象依子類型分——肖像 8、聲音 6、其他 5）。全部選填、不進就緒檢核；審閱「素材包」列顯示檔案數。初版為逐槽上傳格，同日依使用者指定改為自由列表，故登錄端不再計算完整度。與「所有權證明」分成兩個 section 並在副標寫明用途差異（法務驗證 vs 承租方拿到的資產）。主規格 §7.7 已定義 Media Pack 與完整度，各型槽位明細為 §8.10.3 待決，故槽位表待上游裁定後才回寫。連帶修正 `ip-market.html` 九張卡的完整度分母（原一律寫死 x/10，改依 `data-type` 帶各型分母）。 **2026-07-30 真人形象依類型分流欄位（UIA-095，產品變更提案）**：第 1 步在 IP 類型與名稱之間新增條件區塊——選「真人形象」才出現子類型單選卡（肖像／聲音／其他，其他＝未列出的真人形象權利如人設、姓名、簽名），選「肖像」才出現年齡區間起／迄兩個下拉（選項＝不限＋1～100；起 > 迄時自動把另一端對齊，最後選的值優先）。年齡語意＝這批肖像屬於本人哪個時期的形象（使用者確認）。子類型必填（僅真人形象時檢核）、年齡選填；摘要與就緒清單同步。規格 §① 原無依類型分流的欄位，待上游裁定後才回寫。 **2026-07-27 定價步驟改版（UIA-093）**：定價改為「一次性授權費＋收益分潤%」，三個金額欄（費用／獨家／分潤）各自可勾「價格請洽詢」；「最短租期」欄位移除。原內容↓ | 2026-06-15 新建：四步精靈（Type & Info → Usage Rules → Pricing & Earnings → Review）＋發布前檢核 gating；入口＝my-ip Add your IP／ip-market 冷啟動 List your IP。taxonomy／地區互斥／計價單位／Proof 驗證／送出文案標產品待確認（D037） |
| 5.1.5 E-Shop | `e-shop.html` | F1–F4 已覆蓋：tab 切換、搜尋＋狀態篩選（§7.2）、Shop 開關→Hidden、See as fan 分割預覽、Bundles 清單（庫存＝min 成員，D031/UIA-008）、低庫存/售罄實體列補貨入口；互動為前端 demo（UIA-006）；Auctions 建立待 D026。2026-06-15：**D065** F3 工作列加「商店設定」鈕、以 `embed-modal` 全螢幕 popup 內嵌 store-settings.html（不離開清單、關閉保留篩選）；**D064** 限量列 Stock 顯示在庫/上限進度（21/50）、多規格列加「N variants」badge；**D066 F3 改版**：狀態篩選每選項附數量（隨 tab 重算，是否隨搜尋連動待確認）、建立鈕改 context-aware 分割按鈕（`split-button`，主鈕隨 tab）、See as fan 抽出工作列改 F5（UIA-021）；**F2/F3/F5 再改版（UIA-023）**：F2 低庫存改 `.alert--bar` 全寬細條（頂欄之下、sticky 常駐、只留數量＋CTA）、F3 狀態篩選由 select 改第二排 `filter-tabs`（pill＋數量）、F3 類型切換加 `.tabs--brand` 淡橘 pill、**F5 商店預覽改常駐不可關閉的畫面分割**（重用 `preview-panel`、永遠開啟、壓窄 `.main`、非浮層；移除切換鈕與 ✕／Esc；窄螢幕 ≤1100px 改靜態堆疊；標題改「商店預覽」；對齊 spec「常駐右側欄」）。**2026-06-16 F5 預覽內容改用 Fan store 元件（UIA-026）**：粉絲端店面（hero cover＋本月精選＋分頁＋商品格）抽成共用 `partials/fan-store.js`，與商店設定 F1 同源（§6.7）；新增追蹤數/社群/加入社群/本月精選/立即購買/補貨中欄位＝**產品變更提案，待上游核准**（§6.7 禁止預覽引入未定義欄位）。**2026-06-18 PR #4（5.1.5/5.1.5.5 對齊，D083）**：(1) **F4 單一表格、商品列可拖曳重排**＝粉絲端陳列順序（spec §54/§59/§69，由商店設定 F3 移入；商品列 `draggable`＋`.is-dragging/.is-over`＋拖曳提示，無另設排序清單；只 Products，組合/拍賣待確認）；(2) **拍賣清單欄位依生命週期**（§87–91，單表重解 Bid/Activity＋狀況·資格 meta＋Shipped 徽章＋Live 複製連結/Upcoming 編輯/Ended 追蹤履約）；(3) **F6 拍賣即時橫條**（§124–128，Auctions 分頁有 live 才顯示）；(4) **查無符合**空狀態＋Clear（§96，`.eshop-noresult`）。皆前端 demo。**2026-06-18 D084**：F5 商店預覽（See as fan）由「常駐不可關閉」改為**可開關、預設關閉**——F3 工作列加「商店預覽」切換鈕（`#eshop-preview-toggle`，`eye`＋`aria-pressed`）、headerless 預覽加右上浮動 ✕（`.eshop-preview-close`）＋Esc 關閉；移除 `body.preview-open` 預設與 markup `.is-open`，預設關＝管理區滿版（反轉 UIA-023 的「常駐不可關閉」）。**2026-06-22 F3 工作列呈現微調（spec 圖示/文字與搜尋位置非約束）**：商店設定／商店預覽／搜尋三鈕由 `.btn--outline` 改 `.btn--icon`（無框 ghost、向下排 tab/filter 風格看齊，文字進 aria-label＋title）；搜尋自第二排移到上排工作列、預設收合為放大鏡、點擊展開 `.field-pill`（✕／Esc／點外部收起），狀態列由 filter-tabs 獨佔整列；i18n 新增 `data-i18n-title` 在地化機制＋ `e-shop.btn.search`／`e-shop.search.close`。**類型 tab 由淡橘 pill 改回底線式**（移除 `.tabs--brand`、回預設 `.tabs`），狀態篩選改 `.filter-tabs--brand`（淡橘 active＋橘字、數字去徽章泡泡），兩排分層；`.tabs--brand` 仍保留為可選變體。**修正類型 tab 雙底線**（topbar 改 relative、tab 佔整行＋唯一底線、動作列絕對定位浮右）；filter-tabs base 仍灰底（orders/auction-detail 不變）、橘色為 e-shop 專屬 opt-in 變體。**動作列與 tab 底線留間距**、**移除拖曳文字提示**（`.eshop-reorder-hint`，功能改由握把指示）、**頁首→主工作列→清單三段留白加大**、**商品清單列操作收進 kebab（⋯）下拉**（Products/Bundles/Auctions 9 列；Edit／Restock／Copy link／Track 依列入選單，重用 dropdown-menu＋新增 button-item 與 more-vertical icon）、**商品清單後方資料欄收窄**（分類/價格/狀態/庫存，狀態保 96px 容徽章）、**上架開關移入列 kebab**（移除三表 Shop 欄；改 kebab 內互斥選單項「下架商品/上架商品」依上下架狀態顯示，off=Hidden 徽章邏輯沿用）、**F2 低庫存提醒改單句公式**（x 件商品 ( 名稱·前三·… ) 低於庫存門檻，前往查看。；移除獨立 Restock CTA、✕ 放大、名稱用 `--muted-foreground`）、**上下架改「開關列」選單項**（短標籤「在商店上架」＋switch；switch=狀態、點=切換，切換不收選單）、**低庫存提醒收成內容寬置中卡片**（不再滿版）、**狀態 active 底色再淡**（--brand active 9%）、**通知扁平化**（無圓角/外框、單一向下陰影）＋**類型工作列底線改向下陰影**（保留 active 橘線，對齊 Figma 671-2295）。**2026-07-02 D100**：組合（live bundle）列 kebab 加「補貨」，開 `.tabs` 逐成員分別填；商品/組合補貨改立即/計時（見 5.1.5.6，UIA-006）。**2026-07-02 F5 商店預覽改「深色手機版粉絲 app」**（使用者裁示，版型參考 endgame creator 商店手機版、僅視覺證據）：fan-store 元件重寫為手機外殼＋自捲動螢幕（app 頂列＋hero＋app 分頁列＋精選＋子分頁＋商品格＋頭號粉絲＋關於＋底部導航）；螢幕＝fan app 固定深色面（主題例外）；「商店預覽」小標改手機上方 overline；提案欄位擴至 12 項（UIA-026）。**2026-07-06 UI 優化第一輪（B 反饋）**：填充列 demo 資料在地化（繁中分類·合理價格庫存·「剩 N 件」）、競標分類單語化、表頭 Image 欄改空白（對齊 orders/pickup）、縮圖占位改分類 icon（product-list.css 新增占位 icon 樣式＋icons.js 補 5 icon）；**2026-07-31 D161** 粉絲分級欄（誰買得到）改門檻模型：點一級＝門檻設在那一級、該級與其上自動包含，四種合法狀態（全部分級／上榜粉絲以上／超級粉絲以上／僅限核心圈），推翻 07-29 的自由多選；欄位本身無規格出處，記 PG-021。**2026-07-31（同日第二輪，呈現追蹤，依 D164／§7.10 取圖方式）**：F4 商品圖片顯示框比例不變，`.product-list__image` 由方形裁切改為直式原圖置中裁切填滿（76×94）；F5 商店預覽的 `.fan-store__featured-media`／`.fan-store__thumb` 同步改直式。活動列表（`.product-list--events`）不受影響，維持方形。**2026-08-04 釘選（D172）**：F4 三個分頁的清單加「釘選區」——分隔列（`.product-list__divider`）以上為釘選並顯示在商店最前的項目，上限 10、各分頁獨立計；拖曳跨過分隔列＝釘選／取消釘選，列操作選單另有入口；不掛粉絲端精選徽章（使用者裁決），創作者端以分段＋名稱前圖釘辨識；草稿不可釘選、永遠排在整張清單最前；搜尋／篩選中分隔列收起；起始零釘選。上游已以 **D172** 收下這項能力，`5.1.5-電子商店.md` F4／F5／使用情境同步（Plan242 備份）。**2026-08-13（D187）粉絲分級欄收進 Phase 4**：商品分頁與組合分頁的「粉絲分級」欄 Phase 1–3 不出現（欄頭與 13 格掛 `data-feat="full"`＋`data-col="tier"`，`product-list.css` 以 `:has()` 換掉那兩張清單的欄寬）；這一欄無規格出處（PG-021），故走保留 gate。 |
| 5.1.5.1 Product Detail | `product-detail.html` | 已有頁面；費率需引用上游；Restock 鈕接補貨流程（5.1.5.6）。2026-06-15 §2.3/D064：新增「庫存、取貨與購買設定」卡——庫存版本 Edition、取貨方式（物流/QR）、每人限購、商品標籤（重用建立商品元件與 `cp.*` i18n，條件顯示 `data-when-edition`/`-delivery`，皆 demo UIA-018）；多規格逐規格呈現待確認、media/交付細節 R2.1.1 待建。2026-06-29 §2.3/D095：補詳細規格（Specifications，僅實體，可編輯逐筆名稱＋值，promote `spec-row` 元件＋重用 `cp.spec.*`）；主分類移除已打散 Special/Premium（對齊 §7.1/D080）。2026-06-29 §2.3/D096：補完商品圖片／素材（重用 upload-showcase）、數位內容檔案（§4.2 F11）、數位交付說明；主分類 `#pd-main-cat` 切換實體↔數位連動顯隱 `data-pd-cat`；spec 已移除「R 2.1.1 待建」字眼（皆前端 demo，真實上傳／存取防護待上游後端）。**2026-07-02 §2.3/D100**：新增「補貨紀錄（Restock history）」區（重用 `.data-list`，僅實體、最新在上）；Restock 鈕開單一商品補貨面板（立即/計時），送出即 append 一筆紀錄列（demo）。**2026-07-02 §2.3/D105（＋S31.1）**：低庫存門檻欄改**版本切換兩態**——base 唯讀自動 10%（`data-feat-off="S31.1"`）⇄ 升級版可編輯覆寫（`data-feat="S31.1"`）；cheat code「版本」控制（Phase 1 顯示 base、Next+ 顯示可編輯）。S31.1 由 deferred 改為 built-but-gated（UIA-042）。**2026-07-02 §2.3–§2.5/D109**：卡片重分三張——「Product content」只留內容（移出價格·庫存·低庫存門檻）；新增「Price & stock」卡（`#pd-pricestock`：價格/庫存/低庫存門檻＋Edition＋Restock）；原 settings 卡更名「Delivery & buyer settings」（只留取貨/數位交付/每人限購/標籤）。`applyVis()` 改 `document` 範圍；i18n 新增 `product-detail.price.*`、改 `.inv.title`；spec 原 §2.4/§2.5→§2.6/§2.7（UIA-043）。**2026-07-16 §2.14 電影關聯（BR-NEW-1／D138·D140）**：§2.13 標籤與 §2.4 項目引用之間的獨立區塊，內掛共用元件 `partials/film-picker.js`（`data-pd-film-picker`，可搜尋多選：搜尋名稱＋建議 chip 加入＋已選 `chip--removable`，預設示意關聯 2 部）；與 §2.4 項目引用（未上架電影）區隔；候選來自 `js/films-store.js`、前台電影頁呈現與導購屬主站範圍（UIA-053）。**2026-07-16 分頁式佈局（B 反饋）**：單欄長捲改 5 tab（總覽/基本資訊/定價與庫存/交付與取貨/關聯，重用 `tabs.css`）、§2.4 項目引用移入總覽、加庫存健康快照。**2026-07-16 商品資料驅動（B 反饋，UIA-055／PG-017）**：改由 e-shop `?id=<key>` 開啟、讀 `js/products-store.js`（9 單售商品 mock）以 `applyProduct()` realize 對應組合，**移除 devtools `pd-cat/pd-var/pd-edition` 預覽**；§2.7 數位內容檔補齊各型態——影視/單曲/文檔＝上傳格、專輯＝`album-tracks`(seed)、會員卡＝`vip-card`、IP＝素材槽 placeholder（PG-017）；逐品內容為代表性樣本。**2026-07-17 D144**：定價區移除「原價」欄；新增 §2.15 折扣設定（排程特價：開關→特價＋起訖日）與 §2.16 上架設定（上架開關＋定時上架），置 price-stock tab；POPCORN 註解清理。**2026-07-17 上架微調**：§2.16 上架設定選法由 segmented 改 radio-list（位置不動、與創建頁一致）。**2026-07-20 兩欄改版（B 反饋＋Figma 845-10300／Q21）**：單欄分頁改「分頁列全寬＋左右兩欄」——左欄分頁內容、右欄跨分頁常駐唯讀 meta 欄（新元件 `detail-rail.css`），三張卡＝當前庫存（新元件 `stock-bar.css`）／交付與取貨／使用中，鍵值列用新元件 `kv-list.css`；分頁 5→4（總覽併入原基本資訊）；素材改一排四格、欄位順序改 標題→描述→分類→規格；銷售摘要包卡＋四 KPI（已售／營收／扣費後淨利／轉換率）。連帶 Q21 四項全站視覺尺度（區塊標題 18→14、副標與欄位說明壓暗、KPI 底色改 `--input-surface`、頁寬 1280→1056）。**產品缺口 UIA-062～065**：轉換率來源未定、組合引用不在 §2.4、次分類鎖定超出 D137 現況、§2.4 變更影響提示被移除——皆待上游裁決，未回寫 documents/。**2026-07-21 上架設定改收合式**：radio-list 新增 `--collapsible` 變體（Figma 856-22782），收起只顯示目前選項＋chevron、點開才列出全部；行為統一由 `partials/radio-list.js` 接線。圓角與 Q16 相衝，記 STYLE-DECISIONS Q22 待裁決。**2026-07-31 呈現追蹤**：圖片上傳槽改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。 |
| 5.1.5.2 Create Product | `create-product.html` | 已實作三型單頁；2026-06-15 對齊 spec v2.6（D063/D064）補：§4.1④ 多規格 Variations（僅實體，`.segmented` 單/多 + 選項建構器 + 逐規格表，多規格時取代單一價格庫存）、§4.1⑤/§4.2 庫存版本 Edition（不限量/限量，限量補上限 Total Quantity；數位限量補剩餘份數）、§4.1⑥ 取貨方式（物流 重量/尺寸/出貨分類/寄件地 ‖ 現場 QR 領取 領取說明，僅實體）、§4.5 共用設定（每人限購開關＋每人最大購買量、商品標籤）；就緒檢查依狀態動態（多規格→每規格有價格庫存、限量→上限≥在庫、物流→重量/尺寸/寄件地、限購→每人上限）。互動為前端 demo（UIA-016）；硬性必填 vs 建議劃分、選項數上限、單件成本納毛利、QR 核銷、Shipping Categories 來源、限購 enforcement/退款回補、數位限量下載權皆標待確認（D026/D064 §8.12）。2026-06-16：完成「Start selling」就緒後導回 `e-shop.html?posted=1` 觸發新品貼文 popup（→ 5.1.5.7 / D068）。**2026-06-18 對齊 spec v5.0（D081）：拍賣型整個移除，本頁只剩實體／數位兩型**（type 卡、F1 拍賣素材、故事＋物品狀況、真實性、競標設定、競標資格、拍賣出貨、拍賣預覽卡與相關 JS 全部拆走）；舊 `?type=auction` 連結自動轉址 `create-auction.html`。**2026-06-18 對齊 spec v5.1（D084）：§4.5 即時預覽改可開關、預設開啟**——頂部加 Preview 切換鈕（`#cp-preview-toggle`，`eye`＋`aria-pressed`）、預覽 head 加 ✕＋Esc 關閉（markup 維持預設開）；≤760px 靜態堆疊只在 `.is-open` 時套用；嵌入態（`?embed=1`）藏切換鈕。**2026-06-23 §5.2.4 header v2（套用全 6 建立頁 create-product/-auction/-bundle/-event/-project/register-ip）：返回箭頭＋左標題/副標、stepper 改漸層 `.progress-stepper` 移中央內容寬（無 stepper 頁中欄空）、去底線、content＝surface／footer＝surface-shell；儲存狀態（已儲存/儲存中…）＋返回離開確認彈窗（共用 `partials/wizard-chrome.js`：編輯過→儲存並離開/不儲存就離開、未編輯→離開，取消＝✕），前端 demo，記 UIA-028**。**2026-06-29 §5.2.4：全 6 建立頁 header 儲存狀態旁新增「儲存為草稿」二級鈕（`btn--outline btn--sm`，i18n `wiz.savedraft`）；手動儲存同時觸發自動儲存、離開以自動儲存紀錄為準；規格同步 §5.2.4 輸出內容**。**2026-06-27 §4.1 F3.2 定價新增「成本價（Cost）」（D085）：單一規格定價卡加 `#cp-cost`（選填、創作者內部、不對買家顯示），逐規格表 Cost 欄本就有、由「待確認」改確認納入；毛利計算口徑與顯示位置仍待確認**。**2026-07-01 §4 Show it off 互動上傳格（promote `upload-tile.css` 互動狀態＋`partials/upload-tile.js`，opt-in `[data-upload]`）：10 圖片格 選圖→假上傳進度→已上傳→hover 替換/AI 優化/刪除；就緒改監聽 `upload:change`。**AI 優化＝產品變更提案（上游無此功能，ASSUMPTIONS UIA-037，假動作）**；真實上傳/持久化未實作**。**2026-07-01 §4.2 F11.1 音樂專輯多曲目管理（spec v5.3，新 DS 元件 `album-tracks`＋`partials/album-tracks.js`）：數位分類選 Album→內容檔改多曲目管理器（上傳 mp3/mp4→逐曲 封面/曲名/meta/拖曳重排/⋯ 選單 改名·換封面·上傳歌詞〔音訊限定〕·刪除／View Lyrics）；就緒 d-file 改吃曲目數>0。至少一曲·封面必填·歌詞格式·大小上限·試聽·影片曲目歸屬待上游**。**2026-07-01 §4.2 F11.2 會員卡卡面自訂（spec v5.4，新 DS 元件 `vip-card`＋`partials/vip-card.js`）：分類選會員卡→內容檔改卡面自訂器（Text 名稱文字／Image 上傳 PNG logo→合成到平台公版＋即時預覽 OFFICIAL MEMBERSHIP）；內容檔三態切換（會員卡/專輯/單檔）。公版 CSS 近似、實際素材待提供；多公版·名稱字數·logo 硬限制·副標可編輯待上游，D071 權益/存取擱置**。**2026-07-01 §4.2 F11 內容檔（音樂/影片）互動上傳（呈現層，`upload-tile` 內容模式 `data-upload="content"`）：單檔內容格改互動——上傳後 音訊/影片可即時預覽播放＋替換＋刪除（無 AI）；accept 隨分類（音樂 audio/影視 video）。F11 補呈現註記、ASSUMPTIONS UIA-041；真實上傳/持久化未實作**。**2026-07-09 §4.1 F3.2 定價單位切換（spec v5.9／D124／BRD BR-05·I-4）：價格區塊前新增 `.segmented`（沿用既有元件）現金／POPCORN 二選一、作用於整件商品；切 POPCORN 換價格欄 placeholder（`POPCORN 0`）＋隱藏同分類均價提示，JS 沿用 `wireSegmented`、i18n 新增 `cp.priceunit.*`。現金為定價之準；POPCORN 換算率（待定）與收款受 OQ-1 法務閘控＝產品缺口 PG-016、此切換為探索性佔位、不做換算計算**。**2026-07-16 §4.5 F12 電影關聯（BR-NEW-1／D138·D140）**：獨立 `form-section--outlined`（自 §4.4 共用設定拆出，對齊 spec §4.5），內掛共用元件 `partials/film-picker.js`（`data-cp-film-picker`）——**可搜尋多選**（搜尋電影名稱過濾候選＋建議 chip 點選加入＋已選可移除），選填；候選＝前台已上架電影 mock `js/films-store.js`（UIA-053），CS 只建立引用、電影不在 CS 管理**。**2026-07-17 D144**：定價瘦身——移除原價欄與 POPCORN 定價單位切換（價格/逐規格表改純現金 $、`cp.priceunit.*`/`cp.original.*` 移除、`syncPriceUnit` 退場）；新增「折扣設定→排程特價」（開關→特價＋起訖日）與「上架設定→定時上架」（上架開關由預覽欄併入主表單）。**2026-07-17 上架微調**：上架設定移回商店預覽欄（預覽卡下 `.card`）、選法由 segmented 改新元件 radio-list（bump k）。**2026-07-17 折扣微調（產品變更待規格，UIA-060）**：單一規格折扣改「折扣價 ↔ 折扣%」雙欄以定價雙向連動；多規格無單一售價、折扣改「折扣%」套用所有規格並移到逐規格表下方（`data-when-var` 分流）；動到 D144 折扣模型，待同步 spec §4.4 F13。**2026-07-21 上架設定改收合式**：radio-list 新增 `--collapsible` 變體（Figma 856-22782），收起只顯示目前選項＋chevron、點開才列出全部；行為統一由 `partials/radio-list.js` 接線。圓角與 Q16 相衝，記 STYLE-DECISIONS Q22 待裁決。**2026-07-21 商品選項改巢狀層**：切到「多選項」後出現的建構器 `#cp-var-builder` 套新元件 `.nest`（`ds-components/nest.css`）——滿版貼齊母卡左右下三邊＋向上陰影，讀起來像疊在卡片上的另一個平面（Figma 856:27798）。連帶確立層級規則「兩層填色、L3+ 改邊框」（STYLE-DECISIONS Q24），新增 `--nest-surface`／`--shadow-nest-up` 兩個 token、既有 token 零修改。純呈現層改動，不涉產品行為。**2026-07-21 選項編輯器改版（使用者設計圖）**：值由 chip 膠囊改逐值 `.input`（可就地改字、虛線鈕新增一列、Enter 亦可），編輯態加欄位標籤與底部「刪除／儲存」動作列；列去填色改 1px 邊框、新增鈕改虛線（Q24 的 L3 規則首次落地）。空字串值一律過濾、不生空組合。已知限制 UIA-068（改值會讓 `varData` 舊 key 變孤兒）。**2026-07-21 物流欄位改選填（D148）**：§4.1⑥ 四欄只剩重量必填，尺寸與寄件地移除 ＊、退出就緒檢查（`shippingOk()` 只看重量，就緒項文字改 `Shipping weight`）；連帶移除 D145 的第二個收合硬條件（原為保護被收合的必填欄，已無保護對象）。規格先行、UI 後改。**2026-07-31 呈現追蹤**：圖片上傳槽改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。**2026-08-09 呈現追蹤（使用者反饋）**：§4 F1 素材區版面由「主圖大格＋附圖 2×2 並排」改為五格同尺寸、以「四格撐滿一整列」推導尺寸（`.upload-assets--fill`，第五格換行），並改成一次只露出一個空格、填滿才長出下一格（容器層 `[data-upload-reveal]`）；主圖靠角落標籤 `__flag` 表明身分。**張數與必填不變**（主圖／封面 1 張必填＋附圖至多 4 張，spec 5.1.5.2 F1），純呈現層調整。見 STYLE-DECISIONS Q60。**2026-08-10 逐規格圖片（使用者指示，上游無此欄位）**：§4.1④ 多選項規格表列首新增「這個規格的圖片」（`.variant-table--img` ＋ `.variant-thumb`，點開「規格圖片」彈窗換圖），沒上傳就沿用商品主圖。欄位、買家端版位與兩階層歸屬待上游裁決，見 ASSUMPTIONS PG-039。**2026-08-10 第二輪呈現追蹤（使用者指示）**：類型選擇由表單內的 §3 section 改成進場閘門（做法對齊建立活動／建立項目），流程內改類型走「商品資訊」標題列右端的切換鈕；§4 兩個 section 換序（商品資訊 → 展示它）；右側預覽欄改與建立活動同寬（`.preview-split--narrow`）。編輯／嵌入／帶 `?type=` 進場不經閘門。**皆為呈現層調整**，欄位、必填與型別集合不變。**2026-08-09 呈現追蹤（同日第三輪，使用者裁決）**：直式圖片上傳槽全站收斂成單一元件與單一排版（`.upload-assets--fill`：四格撐滿一整列、第五格換行；需要時加 `[data-upload-reveal]` 逐格出現）。`--hero`／`.upload-grid`／`.upload-showcase` 退場。純呈現層，張數與必填不變。見 STYLE-DECISIONS Q60。 |
| 5.1.5.3 Orders | `orders.html` | 已有頁面；2026-06-15 **PCR-001 解決**：每列改兩條狀態軸（履約＋付款·結算，`.status-axes`，§7.2 不混用）；reconcile 提示同步更新。**2026-07-03 D111**：混合訂單列加品項層取貨狀態徽章（待現場取貨），reconcile 下補一句「現場 QR 領取不在此出貨、於取貨管理場次 scanner 核銷、待出貨只計物流」（連 pickup.html）。**2026-07-13**：Export 自 F1 頁首移至 F2 篩選工作列，功能不變。**2026-07-23（B 反饋）**：F3 清單每項資訊各自成欄——`product-list--orders` 由 6 欄改 9 欄（icon／訂單編號／買家／商品＋取貨 meta／金額／出貨狀態／付款狀態／日期／actions），原擠一格的編號＋買家＋商品拆三欄、狀態欄的 `.status-axes` 雙徽章拆 `__ship`／`__pay` 兩欄；純呈現層、更貼合 §7.2 兩軸分離，產品規則不變。**2026-07-23（B 反饋，續）**：欄位重排＝icon／訂單編號／訂單內容（「商品」改名）／買家／金額／付款狀態／出貨狀態／日期／actions；**現場取貨由品項層 meta 升為出貨狀態軸徽章「待取貨」**（`orders.status.pickup`），混合訂單出貨欄同放「待出貨＋待取貨」兩顆、水平不換行——此舉重新詮釋 D111 的「品項層、不改訂單層狀態」定位，記 ASSUMPTIONS UIA-080、待上游回寫本節 D111。**2026-07-23（B 反饋，第三輪）呈現細節**：訂單內容改每項一行白字（`__line`，fs-13、最多 4 行 …）、縮圖改真實商品照（`.product-list__image`，Q20 訂單 scoped 例外）、縮圖與編號拉開距離、編號旁加很小的複製鈕（`__copy`）、整列可點開啟訂單（比照 creators）；皆呈現／互動層、產品規則不變。**2026-07-23（第四輪）**：使用者裁示「訂單列表不需要商品圖片」，移除縮圖欄（9→8 欄，Q20 訂單 scoped 例外隨之失效）；訂單內容各品項行距加大（`__line` 間 `margin-top:var(--sp-6)`）。 **2026-08-07（B 反饋／bug 根因）清單改由 store 產生、點列帶訂單編號**：四列不再寫死在 HTML，改讀 `js/orders-store.js`（與訂單詳情同一份資料），列上帶 `data-order-id`；整列點擊與 kebab「開啟訂單」都導向 `order-detail.html?id=<編號>`（沿用站上 `project-detail.html?id=` 慣例），原本寫死成不帶參數的 `order-detail.html`。**狀態字樣收斂**：#ZT-10481 的履約徽章由規格外的 `Delivered／已交付`（`orders.status.delivered`，不在 §7.2 訂單狀態表內）改為 §7.2 的 **已完成（Completed）**，該 i18n key 刪除。清單品項摘要改用與詳情頁共用的 `od.itemN.name` ＋ 數量，`orders.rowN.*` 系列孤兒 key 刪除、persona 覆蓋一併搬到新 key。產品規則不變。 **2026-08-07（B 反饋）demo 訂單補到 12 筆蓋滿狀態矩陣**：`orders-store.js` 由 4 筆補到 12 筆，§7.2 七個狀態全數出現、F2 七個頁籤點下去都有結果（實測 12／1／8／3／4／3／3）；履約組合五種齊備（純寄送／純現場取貨／純數位／混合寄送＋取貨／實體＋數位同單），邊界情境含多品項、部分退款、整筆退款、爭議中、待付款。**篩選頁籤歸屬改由 `pay`／`fulfil` 推導**（不再手寫 `filters`，避免徽章與頁籤兩份資料漂開；退款與爭議併同一分組＝§7.2 明文允許）。**F1 四張 KPI 卡改由同一份資料算**（`kpi()`，口徑照 F1：待出貨只計物流、待處理＝未付款、退款／爭議＝refund 分組、近 30 天完成以資料最新日為基準），取代原本寫死的 8／3／2／214。**待付款訂單履約軸為空**（尚未付款＝尚無履約動作），清單該格以破折號佔位。i18n 新增 `orders.pay.unpaid`／`orders.pay.refunded`／`od.item5–11.name`（含 nick persona 覆蓋）。產品規則不變。|
| 5.1.5.3.1 Order Detail | `order-detail.html` | 已有頁面；2026-06-15 §2.2 兩條狀態軸 `.status-axes--labeled`（PCR-001 解決）；§2.5/D064 取貨方式履約分支 demo 切換；§2.3 每人限購結果唯讀呈現（承接結果不重設）；§2.6/D041 v1 退款鈕停用＋說明（爭議保留、退款回補口徑見 §7.3）。**2026-07-03 D111**：§2.3 品項列加取貨資格狀態（待取貨／所屬場次／核銷紀錄入口，連 pickup.html）；§2.5 出貨 popup 的 QR 分支改為「不在此手動 Mark received、由取貨管理 scanner 核銷回寫」＋隱藏確認鈕＋前往取貨管理入口（改寫 `od.qr.body`）。**2026-07-16 對齊 spec 5.1.5.3.1（O18／O23／D041，UIA-057）**：**§2.3 品項與金額細分**加 `.od-subhead` 三具名子塊（品項明細 §2.3.1／金額拆解 §2.3.2／收入對帳 §2.3.3，page-specific 局部 class）；**退款改彈窗（O23）**＝原常駐「Refund & dispute」card 撤除，改 `#od-refund-modal`（重用 `.payout-modal`／`.payout-dialog` 外殼，比照出貨彈窗），退款範圍 Full／Partial `.segmented`＋部分退款勾選品項 `.od-refund-check`＋即時試算 `.od-amt`＋庫存回補/收入影響/爭議區隔 `.field__hint`，皆前端 demo；**退款鈕最終形態**＝頁首 Refund 鈕 `#od-refund-open`（保留 `data-feat="O18"`）移除 disabled、可點開彈窗，**Phase 1 由 O18 gate 隱藏＝守 D041「v1 不開放創作者主動退款」**（真實退款經 Earnings §7.3）；**爭議降為狀態**＝dispute 由常駐區塊降為 §2.2 Payment·settlement 軸的 disputed 狀態值（示範訂單 #ZT-10482＝Paid、不觸發顯示）＋彈窗末尾爭議區隔說明，守 PCR-001 兩軸不混用。i18n 新增 `od.items.sub.*`／`od.refund.*`（`od.refund.title` 改「Refund」）。**2026-07-16（後續，取代上段 §2.3 具名子塊做法）**：§2.3 三子塊與 §2.4 買家改用 `.form-section--outlined`（棄用 `.od-subhead`，手法對齊商品細節頁/建立頁），移除 `.bento` 並排、改單欄堆疊；元件層配色對比優化——`field__hint`／`badge--warning`／`segmented` 軌道邊界修復（design-system.md 同步）；**§2.3.1 品項名稱改開「商品快照」popup（§2.7／D143）**：新增 `#od-snapshot-modal`（重用 `preview-card` 靜態渲染＋所購規格條件顯示＋管理入口連回 5.1.5.1），不再直連商品管理頁；i18n 新增 `od.sec.*`／`od.buyer.sub`／`od.snap.*`；ASSUMPTIONS 補 UIA-058。**2026-07-23（B 反饋）改兩欄版型＋精簡**：改用 `detail-rail` 版型殼（`.page--narrow`＋`.detail-grid`）＝**左主欄品項明細／右欄買家資訊**；金額拆解併入品項明細（商品列表下）、收入對帳獨立區塊撤除改右下角常駐入口（`od.amt.cta`）、移除限購說明句（`od.limit.note`，Limit 徽章仍在）、買家→買家資訊（`od.buyer.title`）。純呈現重組，商品/金額口徑/買家資料不變；spec §2.3.1/2/3＋§2.4 內容責任未動、未回寫上游。**2026-08-06（B 反饋）撤除出貨彈窗的 QR／數位 demo 分支＋物流商加國際選項**：`#od-ship-modal` 拿掉 `.segmented#od-ful-view` 分頁與 `data-od-ful-branch="qr"`／`="digital"` 兩塊內容（`setBranch()` 隨之簡化，固定為 shipping 單一分支），因為本頁訂單是配送型訂單、履約方式單一決定，分頁預覽只是 demo；QR／數位兩種履約方式本身（pickup.html／O22）完全未動。物流商 `<select id="od-carrier">` 新增 `<optgroup>` Local／International，International 含 FedEx／DHL／UPS。i18n 孤兒 key 清理：`od.ful.qr`／`od.ful.digital`／`od.ful.shipping`／`od.qr.title`／`od.qr.body`／`od.qr.session`／`od.qr.manage`／`od.qr.note`／`od.qr.mark`／`od.dig.item`（含 persona 覆蓋）／`od.dig.meta`／`od.dig.available`／`od.dig.note`／`od.badge.received`，皆先 grep 全站確認零消費才刪 **2026-08-07（B 反饋／bug 根因）本頁改依 `?id=` 資料驅動**：資料來源 `js/orders-store.js`（四筆示範訂單，與訂單清單同源）；沒帶 id 用第一筆當合理預設，查無 id 顯示 `empty-stub` 查詢失敗（比照 `event-detail.html`），不拿別人的訂單頂替。頁首（編號／麵包屑／摘要／兩條狀態軸）、§2.3.1 品項列、§2.3.2 金額拆解（數位訂單無運費列、同幣別不顯示換算列）、§2.4 買家（純數位訂單收起「寄送地址」整列）、§2.6 退款彈窗的可退品項與整筆金額全部依該筆訂單產生。**§2.5 出貨與履約 popup 依訂單型態二選一（非分頁）**：實體／混合訂單顯示物流商＋追蹤碼＋「標記出貨」（已出貨的訂單帶出憑證＝已填的物流商與追蹤碼，§2.5「完成後呈現對應憑證」）；**純數位訂單（#ZT-10481）不顯示物流商／追蹤碼**，改成唯讀的「數位交付」區塊（`kv-list`：交付方式／交付狀態／交付時間／下載次數）、頁首主操作改稱「數位交付」、彈窗只有關閉鈕——依 §2.5 區塊狀態與 §2.3「數位商品呈現下載／存取權限的交付狀態」。規格未給數位交付專屬狀態名，狀態值沿用履約軸的 Completed，記 ASSUMPTIONS UIA-107。**同義不同詞的狀態 key 併回單一來源**：`od.badge.toship`／`od.badge.paid`／`od.badge.shipped`／`od.item.deliver`（zh 為「待配送」）／`od.item.pickup.pending` 五個重複 key 刪除，全部改用 `orders.status.*`／`orders.pay.*`；`od.sub` 改帶變數的 `od.sub.placed`／`od.sub.placed1`；`od.item1.meta`／`od.item2.meta` 改由分類 key＋單價組出、key 刪除。 **2026-08-07（B 反饋）頁首重排＋副標拆開搬家**：頁首序改為 麵包屑 → `<h1>` 訂單編號 → 狀態列（與商品／組合／拍賣三個詳情頁「標題在上、徽章在下」一致）；原副標「建立於 {date} · {n} 件品項」撤除，件數改掛品項明細區塊標題右側（`form-section__head--actions` ＋ `.chip--static`，`od.items.count`／`od.items.count1`）、下單時間改進右欄新增的「訂單資訊」卡（`od.info.title`／`od.info.placed`，只放規格 §2.2 已有的下單時間一列、不新增規格外欄位）。兩條狀態軸的軸標籤移除、改 `.status-axes--split` 細分隔線（見 BUILD-SPEC Status axes）。i18n 刪 `od.axis.fulfil`／`od.axis.payment`／`od.sub.placed`／`od.sub.placed1` 四個孤兒 key；`od.sec.items.sub` 去掉開頭重述區塊標題的「品項」。純呈現層，產品規則與資料口徑不變。 **2026-08-07（A spec）補齊 §2.5 的第三種區塊狀態＋退款結果呈現**：**純現場 QR 領取訂單**（#ZT-10484）新增 `data-od-ful-block="pickup"`——§2.5「純取貨訂單不顯示物流商／追蹤碼欄位，改引導至取貨管理查看或處理核銷」原本沒有實作；彈窗改唯讀（品項狀態徽章＋說明＋前往取貨管理入口，重用 `kv-list`／`card__link`），頁首主操作改稱「取貨狀態」（`od.btn.pickup`）、只留關閉鈕，做法與純數位那塊平行。**已退款訂單**在金額拆解下方加一行退款事實（`od.refund.issued.full`／`.partial`，帶 `{amt}`／`{on}`）——§7.2 規定單筆訂單只屬一個付款・結算狀態，部分退款在徽章上一樣是「已退款」，靠這一行分辨整筆或部分；金額吸收與結算口徑仍以 Earnings 為準、本頁不重算。**待付款訂單**履約軸與品項出貨欄以破折號佔位，「標記出貨」停用（§4 情境 1 為「確認付款後」才出貨）。**bug 修正**：品項出貨欄原本只判斷「有沒有已出貨」，已完成訂單的物流品項會顯示「待出貨」、與頁首自相矛盾，改為已完成 → 已出貨 → 待出貨逐級判斷。#ZT-10482 寫死的支付費 −$1.30（＝2.32%）更正為 −$1.34、淨額 $51.26，12 筆共用同一條 §7.6 算式。產品規則不變。 **2026-08-07（A spec／C 撤除）§2.3.2 換匯資訊升成表內一列、§2.3.3 對帳說明句撤除**：跨幣別（§2.3.2「原幣、換算幣、匯率、時點」）原為金額拆解表外的一句 `field__hint` 註腳（`od.amt.fx`），改用該表既有的列結構寫成一列——左欄 `od.amt.fxpaid`「買家實付」、右欄原幣金額，匯率與時點放左欄下方小字（`.od-amt__sub`，page-specific，無新元件）；位置在商品金額＋運費之後、費用扣項之前（該列＝上兩列合計，亦為費用計算基礎；接在淨額後會破壞 `:last-child` 收尾樣式）。**同幣別訂單不顯示此列**（規格明文）改由資料驅動：`js/orders-store.js` 的 `fxKey` 換成 `fx`（買家付款幣別＋原幣金額＋匯率，同幣別為 `null`），12 筆中 #ZT-10482 顯示、其餘 11 筆不顯示；原註腳的 NT$1,764 漏算運費，更正為 NT$1,921.50。§2.3.3 的「金額與收入管理對帳——訂單詳情不自行重算」整行撤除（`od.amt.hint` 刪除，全站零消費）——「不自行重算」是規格寫給實作的原則，創作者要的「去哪裡核對」已由常駐入口 `od.amt.cta`「在收入管理查看 →」承擔（該入口保留）。i18n 刪 `od.amt.hint`／`od.amt.fx`、新增 `od.amt.fxpaid`。ASSUMPTIONS 補產品缺口 PG-034（數位商品退款後的存取權限未定義）。產品規則不變。|
| 5.1.5.11 Pickup Management | `pickup.html`、`pickup-detail.html`、`scanner.html`、`partials/pickup-session-modal.js`、`ds-components/pickup.css`＋`scanner.css` | **新頁（2026-07-03 D111／Plan170）**。E-Shop 下與訂單管理並列、header 下拉第三項。**2026-07-06 D115–D117**：頁面重整為只留 F1 頁首／F2 摘要／F3 清單工作列（含建立場次入口＋狀態 filter-tabs＋搜尋）／F4 取貨清單（分批載入）。**2026-07-06 對照原型發現並拆分單頁 tab**：原本 pickup.html 單頁用 `data-pickup-view` 切 list/detail、detail 內再用 `.tabs` 切 items/roster/log，皆程式內部狀態、無獨立網址。**2026-07-06 D121（5.1.5.13 整份退役）**：拆成兩個真實檔案、真 `<a href>` 導覽——`pickup-detail.html`（5.1.5.15：場次基本資訊、Scanner URL 與密碼、核銷紀錄、**F4 取貨/入場名單完整管理版**含搜尋/狀態篩選/標記有問題/反轉）／`scanner.html`（5.1.5.14：F1 掃描、**F2 可核銷項目正式定義**、**F3 唯讀簡化名單**供 QR 無法掃描時人工核對，Scan/Items/Roster 分頁互切、不連出其他工作台頁面）。兩頁皆為單一固定 demo 場次（Taipei signing），不依 `?id=` 動態切換，與其他 detail 頁（如 product-detail）現況一致（UIA-013 先例）。**F3 建立/編輯場次 popup** 共用於 pickup.html＋pickup-detail.html＋create-product＋product-detail（`.payout-dialog`＋`.pickup-select` 多選＋名稱/地點/開始·結束時間〔含結束>開始驗證〕/取貨說明/scanner 密碼→scanner URL 結果步；商品脈絡開啟預先勾選該商品）。全前端 demo：QR＝faux SVG、密碼接受任意非空、資料為 sample（UIA-046）；真實 QR/核銷後端/scanner 認證與鎖定/離線同步/取貨資格狀態機回寫待工程。**D112**：取貨場次移除「草稿」狀態（§7.2）。**D113**：F1（現 F2）移除「待設定取貨場次」KPI，三卡改 `bento--span-4` 平分。**D119**：補「未到場 No-show」狀態＋F4 分批載入規格。**2026-07-06 D122（部分推翻 D119）**：核銷改二元制——只留 待核銷／已核銷（＋取貨場次待設定／已取消退款），移除 部分核銷／有問題／未到場。pickup.html Issues KPI→今日已核銷、清單去 Issues 欄與 No-show 統計；pickup-detail F4 名單去 No-show／Issues 篩選與列、去「標記有問題」只留反轉；scanner Items 去 Issues、Roster Issue 列改已核銷、SCEN 保留「已領取過／不在此場次」即時提醒。**2026-07-06 D123**：pickup-detail「取貨與入場名單」與「核銷紀錄」由上下堆疊改為 `.tabs` 分頁切換（名單預設在前、紀錄在後）；規格 5.1.5.15 改寫成三 F 項（F3 分頁切換／F4 取貨與入場名單／F5 核銷紀錄），5.1.5.11／5.1.5.12／5.1.5.14 交叉引用同步改號。**2026-07-23（B 反饋）F4 場次欄拆欄＋名稱三行**：清單場次欄由「名稱＋內容疊一格」拆成兩欄——名稱欄改三行（場次名稱／取貨地點灰字／時間灰字），內容（商品·票券 meta）獨立成新欄；原「取貨地點與時間」欄併入名稱欄後移除，欄數不變（8 欄）。純呈現重組，沿用 `__title`／`__meta`／`__cell`、無新增 CSS；i18n `pk.col.loctime`→`pk.col.content`、`pk.rowN.loctime`→`pk.rowN.loc`＋`.time`。產品規則不變 **2026-08-01 使用者裁決（詳情頁重排）**：`pickup-detail.html` 第一屏收斂成 `.pickup-board`（左＝地點時間＋領取項目，右＝核銷進度／這場結果＋`.scanner-hand` 掃碼網址密碼）；名單改 `.ztor-table`＋`.pickup-roster`，欄位對齊 5.1.5.15 F4 七欄；核銷紀錄改 `.payout-dialog` popup（提案推翻 D123 雙分頁，記 UIA-100）；`partials/pickup-session-modal.js` 由三頁籤改兩步（`.pks-step`），掃碼密碼改選填並預先產生（提案偏離 5.1.5.12 F3 必填，記 UIA-100），掃碼網址重設／停用移進編輯模式；撤除 result step、`Start scanning` 主鈕與 QR 圖。 |
| 5.1.5.4 Create Bundle | `create-bundle.html` | 已實作；2026-06-15 §6.3/§6.4 補：套組庫存＝min(成員) 即時顯示（`data-stock`）、固定價≤成員合計驗證＋省下金額（超過擋 Create）、成員限量/多規格相容性提示（`data-edition`/`data-variants`）、Create gating 加 name＋price；皆顯示層 demo（UIA-019）。2026-06-29 §4 F3 折扣改雙欄連動（折扣後價↔折扣趴數，依成員合計換算、最後編輯為準、折後價>合計擋建立），spec v1.5／D088。2026-06-29 v1.6/D089：F1 加素材(upload-tile)＋描述、F4 限量改庫存(Edition 不限量/限量＋唯讀在庫=min 成員＋限量上限)、新增 F7 排程特價(啟用 toggle＋起訖日，啟用時必填且結束晚於開始)；素材/描述為 demo 不納 gating（UIA-033）。2026-06-29 v1.8/D091：素材／組合資訊拆兩區段＋F8、庫存改 selection-card、加即時預覽欄（preview-col 粉絲卡即時更新）＋上架開關（F6 確認）、footer 右對齊、F7「排程特價」改名「販售排程」。就緒檢查（§4 F6）spec 仍待確認、未建；F7 特價價格/時區、成員票券（§8.9）待補。**2026-07-02 v1.9/D102（撤 D088）：組合定價移除創作者設定價/折扣，改成員自動加總、唯讀 `.cb-autoprice`（多規格成員→「從 $X 起」）；Create gating 去檢價（名稱＋≥2 成員）；bundle-detail(5.1.5.9 v1.6) 定價同步唯讀；結帳依所選規格實際計價待上游。**2026-07-02 v2.0/D103（修訂 D102）：折扣補回——自動 base 上加單一「折扣 %」欄（選填 0–100），粉絲付＝base×(1−折扣%)，有折扣顯示折後價＋劃掉 base；不恢復固定價/絕對折後價**。**2026-07-17 D144**：F7「販售排程」改名回「排程特價」並歸入「折扣設定」區塊（反轉 D091）；新增「上架設定→定時上架」，上架開關由預覽欄併入主表單。**2026-07-17 上架微調**：上架設定移回組合預覽欄（預覽卡下 `.card`）、選法由 segmented 改 radio-list。**2026-07-21 上架設定改收合式**：radio-list 新增 `--collapsible` 變體（Figma 856-22782），收起只顯示目前選項＋chevron、點開才列出全部；行為統一由 `partials/radio-list.js` 接線。圓角與 Q16 相衝，記 STYLE-DECISIONS Q22 待裁決。**2026-07-31 呈現追蹤**：圖片上傳槽改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。 |
| 5.1.5.5 Store Settings | `store-settings.html` | 已實作（spec v14 / D036 功能頁）：F1 頁首與動作（含 See as fan）、F2 店面門面常駐、F4 付款、F5 出貨；品牌素材、幣別歸屬屬有效產品決策；tab 與畫面分割為非約束呈現（BUILD-SPEC）。**2026-06-16 F1 See-as-fan 預覽改用 Fan store 元件（UIA-026）**，與 E-Shop F5 同源（§6.7）。**2026-06-18 PR1（D083，反轉 D031）：F3 商品陳列移出本頁**——粉絲端陳列排序改由電子商店 5.1.5 F4 末段管理；本頁設定群組 3→2 分頁（Payment｜Shipping），`.ss-order` 清單樣式 promote 成 `ds-components/order-list.css`。**2026-07-02 F1 預覽隨 fan-store 改版為深色手機**（同源自動生效，見 5.1.5 列＋UIA-026）。**2026-07-09 幣別重整（spec v16／D125）：(a) 幣別由 F2 店面門面抽出、獨立成 F6 · 幣別；(b) F6 幣別與付款／出貨並列為第三個設定 tab（付款｜出貨｜幣別）——原型把幣別 select 從身分帶 meta 行搬進設定群組 tabpanel（`data-ss-tab/panel="currency"`），i18n 加 `store-settings.group.currency`／`store-settings.currency.hint`；(c) 選項由原型佔位 USD/EUR/GBP/JPY/TWD 校正為規格四種 HKD／TWD／SGD／USD；顯示鎖定方式待上游**。**2026-07-17 D144**：F6 幣別說明移除與 POPCORN 單位的對照（POPCORN 定價單位已自產品移除）。**2026-08-04 新增 F7 商品規格（spec v17／D171）**：設定群組 3→4 tab，第四組＝商店層級的**尺寸表範本庫**（一店可建多份具名範本，建立商品時挑一份套用）。清單（名稱／商品類型／套用中／kebab 編輯·複製·刪除）↔ 單一範本編輯兩檢視互切；編輯欄位照 F7.2＝尺碼制（國際／US／EU／JP）、量測單位（公分／英吋）、尺寸表（列＝尺碼、欄＝量測項，兩軸可增刪，新元件 `size-chart-editor.css`）、版型建議、身高體重參考、量測方式、免責說明。商品類型本版只實作「衣服」、做成唯讀（其他類型欄位上游未定義）。**兩項未覆蓋且不自行補**：商品層選用範本的欄位 `5.1.5.2` 尚未定義（PG-032，範本庫目前無下游消費點、清單「套用中」為假資料）；主尺寸表用平量欄而量測說明教圍度、基準不一致（PG-031），UI 不做校驗也不暗示對應。demo 手法（四制共用同一份數值等）見 UIA-106**2026-08-10 新增 F8 優惠碼（D183）**：第五個設定群組，清單 ↔ 單組編輯兩層檢視；兩種碼（自用／代理），代理碼多出推廣者、分成與唯讀的代理銷售紀錄。折扣型態值域、使用次數上限、適用範圍、推廣者挑選器四項因規格未定而不做（ASSUMPTIONS UIA-072、主規格 §8.22）。 **2026-08-11（D184）由彈窗改為獨立頁面**：全域導覽＋麵包屑＋頁首動作；設定群組改主欄橫向分頁四組（收款／銷售預設〔幣別＋出貨〕／商品規格／優惠碼），店面門面改右側常駐直軌可就地編輯；e-shop 的 embed-modal 撤除、按鈕改導頁，返回時還原清單狀態。 **2026-08-13（D185）代理碼收進 Phase 4**：優惠碼分頁各版本都有，但型別切換列、代理專屬區塊（推廣者／分成／代理銷售紀錄）、清單的代理碼列與「自用」meta 標籤改掛保留 gate `data-feat="full"`；分頁開場白做成 `data-feat`／`data-feat-off` 成對兩句，低版本只講折扣本身。`feature-scope-map.md` 補登 `S51`。 **2026-08-13（同日）**：F7 尺寸指南的範本編輯改成彈窗（`#ss-spec-modal`，清單↔編輯不再同頁互換，兩顆「返回清單」退場）；F6 幣別鎖成唯讀、預設一種、「選擇幣別」空選項退場（D186）；另修 D184 改版遺留的兩個斷點——F7 互動 JS 查的還是舊選擇器（按鈕全沒反應）、收款與出貨的 `data-feat="S05"`／`"S06"` 掉了。 |
| 5.1.5.6 Restock | `e-shop.html`、`product-detail.html`、`partials/restock-modal.js`（`createRestock`→`openOrder`） | popup 對話框（重用 payout 外殼＋segmented/data-list）。**補貨單模型（v1.4/D104）**：<br>單據層（整單填一次）＝方式 `.segmented`（立即/計時）＋供應商/預計到貨/備註；<br>品項數量列 `.restock-lines`／`.restock-line`（識別＋狀態 badge＋目前庫存/門檻＋數量＋即時補後）。品項依入口攤平：單一規格 1 列／**多規格逐啟用規格列**（5.1.5.2 §4.1 F3 路線 B，`.restock-lines__group` 商品名）／**組合＝成員商品用 `.tabs` 分開**（一成員一 tab，tab 內該成員規格矩陣；多規格成員展開規格列、2 選項再分組）。**數量留空＝不補**、跨 tab 保留；商品規格不用 tab、只有組合成員用單層 tab（D106，解 D101 兩層 tab）。方式：立即→In stock；計時→Restocking＋預計到貨必填＋到貨確認→In stock。**補貨紀錄**逐品項呈現於細節頁 §2.3（`.data-list`：數量/時間/供應商/狀態；多規格標規格）。建議補貨量、庫存/狀態重算口徑、同單混合方式、部分收貨（§7.2）標待補（UIA-006/007），本輪顯示層 demo（2026-07-02 D106 成員 tab；沿革 D104 補貨單／D101／D100）。**2026-07-02 D105**：列 meta 的低庫存門檻由寫死 5 改為 `lowThr(cap)=ceil(cap×10%)` 逐品項導出（無 cap 列隱藏該 meta；UIA-042）|
| 5.1.5.7 New Product Post | `e-shop.html`、`partials/product-post-modal.js`、`create-product.html` | 2026-06-16 落地（spec 5.1.5.7 / D068）：建立商品完成→導回電子商店清單以 `?posted=1` 彈出撰寫 popup（`.payout-dialog` 外殼＋**重用群發 composer** `.msg-*`：受眾／標題≤120／內文≤2000／token／排程，message-modal.css），本檔只加 F2 商品附件卡 `.npp-product` 與略過路徑；無頁首（D067）。發布為 demo（通知粉絲＋寫入 Fans CRM，引用 5.1.7.1／5.1.2.2 §4.9）。可否不附/換商品、預設受眾、公開動態邊界、composer 跨模組共用、與群發去重、未存提示皆待確認（§8.13，UIA-024）|
| 5.1.5.8 Auction Detail | `auction-detail.html` | 已建（§2.1–§2.7）：競標生命週期三階段、物品摘要、競標概況 KPI、出價活動（filter-tabs＋Reserve met）、拍賣資訊一覽、Preview 接 fan-store。競標產品語意（密封終局、保留價流標、結標履約等）標待確認（§8.14）。**2026-07-17 D144**：定時開拍設定在建立拍賣頁；本頁 Started/Ends 已反映開始時間，無結構性改動。**2026-07-27 版型對齊商品明細**：由 bento card 改成 `page--narrow` ＋ tabs ＋ Detail rail 殼（§2.1–§2.7 內容一項未增減，只重新分組：總覽＝§2.5＋§2.4／出價＝§2.6／拍賣資訊＝§2.7；右欄放唯讀競標狀態）。規格 §3 明定分欄與版面由 UI 端決定、非約束。**2026-07-27 §2.3 撤除**：競標生命週期三階段列與 §2.2 狀態徽章重複，經使用者裁示移除（含專屬樣式與三個 i18n key）；規格 §2.3／§3 顯示序第 3 項仍在，衝突記 `ASSUMPTIONS.md` PG-019 待上游裁決，未回寫 documents/ |
| 5.1.5.9 Bundle Detail | `bundle-detail.html` | 已建（§2.1–§2.5）：組合內容（名稱/描述/素材/成員/定價/庫存/排程特價）、銷售摘要＋空狀態、成員影響、See-as-fan 預覽。2026-06-29 同步 5.1.5.4 v1.6/D089：加描述＋素材（upload-tile）＋庫存 Edition（selection-card；唯讀在庫=min 成員）＋販售排程（原「排程特價」，D091 改名）；%off 改折扣後價↔折扣趴數雙欄。成員票券口徑（§8.9）、F7 特價價格/時區待補。**2026-07-17 D144**：§2.3「販售排程」改名「排程特價」歸入折扣設定（反轉 D091）；新增「上架設定」（上架開關＋定時上架）。**2026-07-17 上架微調**：上架設定選法由 segmented 改 radio-list（位置不動、與創建頁一致）。**2026-07-21 上架設定改收合式**：radio-list 新增 `--collapsible` 變體（Figma 856-22782），收起只顯示目前選項＋chevron、點開才列出全部；行為統一由 `partials/radio-list.js` 接線。圓角與 Q16 相衝，記 STYLE-DECISIONS Q22 待裁決。**2026-07-27 版型對齊商品明細**：由 bento card 改成 `page--narrow` ＋ tabs ＋ Detail rail 殼（§2.1–§2.5 內容一項未增減，只重新分組：總覽＝§2.4＋§2.3 名稱描述素材／銷售設定＝§2.3 價格・折扣・庫存／成員＝§2.3 成員清單＋§2.5；上架設定依 STYLE-DECISIONS Q25 移入右欄，另加唯讀「組合概況」卡）。規格 §3 明定分欄與版面由 UI 端決定、非約束。**2026-07-31 呈現追蹤**：圖片上傳槽改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。 |
| 5.1.5.10 Create Auction | `create-auction.html` | **2026-06-18 新建（spec 5.1.5.10 v1 / D081）**：拍賣獨立流程，頂部選種類（實體／數位／活動，只影響分類與 F6 交付）；F1 素材＋F2 故事・物品狀況（四級・預設全新・分類用所選種類 §7.1 次分類）＋F3 真實性（合照＊／來源說明／證書）＋F4 競標設定（起標價＊／保留價／時長 3·5·7＝5 預設／密封終局＋5% 增額）＋F5 競標資格（核心圈預設／超粉／全部）＋F6 交付（實體＝得標者付運費＋出貨時限｜數位＝下載 stub｜活動＝入場 stub）＋商品標籤；右側即時預覽「Now fans see it」＋就緒檢查「Ready to start auction?」（8 項）。沿用 create-product 元件與 `cp.*` i18n、新增 `ca.*`；競標語意（密封終局/保留價流標/資格門檻/數位·活動交付）標待確認（§8.14 / PG-014）。入口＝e-shop F3＋Auctions 分頁；舊 `create-product?type=auction` 轉址至此。**2026-07-17 D144**：新增「上架設定→定時開拍」（上架開關由預覽欄併入主表單＋定時開拍時間）；拍賣不設排程特價。**2026-07-17 上架微調**：上架設定移回拍賣預覽欄（預覽卡下 `.card`）、選法由 segmented 改 radio-list（不開拍／立刻開拍／定時開拍）。**2026-07-21 上架設定改收合式**：radio-list 新增 `--collapsible` 變體（Figma 856-22782），收起只顯示目前選項＋chevron、點開才列出全部；行為統一由 `partials/radio-list.js` 接線。圓角與 Q16 相衝，記 STYLE-DECISIONS Q22 待裁決。**2026-07-31 呈現追蹤**：圖片上傳槽改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。 |
| 5.1.6 Events（含 F5 活動詳情與營運） | `events.html` | 已有清單；2026-06-15 副標對齊；D060/D033(c) 移除 Drafts 時段分頁、草稿改狀態軸（跨時段）；詳情與營運（F5）2026-06-15 新建 event-detail.html（7 分頁：總覽/票種/名單報到三色/退款招待/通知/成本營收/系列＋轉售P2），規格已定部分落地、退款吸收順序與轉售 Phase2 標待補；由 events 已發布列進入。**2026-07-24（D149）**：清單 Upcoming 新增一筆共看派對（Watch Party）樣本列（線上、以人數上限計、單一入場券），KPI Total events 11→12；共看派對不套 QR 報到／退款／系列（§4.7）。**2026-07-31（呈現追蹤，依 D164／§7.10）**：清單縮圖 `.product-list--events .product-list__image` 維持 `1:1` 方形，是全站顯示端改直式收斂唯一保留的方形版位（其餘 6 支同族元件皆已改直式）。**2026-08-09（使用者指示）**：工作列的「＋ 新增活動」改成 split-button（沿用 e-shop 建立商品的既有元件），下拉兩項＝建立活動／建立活動（舊版流程），第二項指向正式化前的備份頁 `create-event-legacy.html`；空狀態卡片的 CTA 維持單一按鈕、指向新版。備份頁未列入 §3.2 產品地圖，見 ASSUMPTIONS BKY-003 ② **2026-08-11 活動詳情與建立流程同步（使用者指示「將建立活動的流程，同步改到活動詳情頁」）**：`event-detail.html` 總覽的「活動人數」（最少／最多）整組撤除（對齊 ATT-002），KPI「已售」與系列清單售罄判定的分母改用票種張數加總；「開放入場」升級成建立流程同款——時間與「開演前 N 分鐘」兩格互算（基準＝開始時間）、任一有值才出現「比這個時間早到的人」二選一（每筆活動＝一場，政策存活動身上 `ev.early`）；票種卡由三欄（名稱／票價／張數）擴成五欄，補**手續費**與**提前入場（分鐘）**，提前入場的說明即時換算實際入場時間、改總覽的開放入場會即時跟上。`events-store.js` 種子補 `early`／`fee`／`earlyMin` 三個欄位（示範一筆）。**票務商品（捆綁包）本輪未搬**——store 無捆綁資料、詳情頁無對應分頁，見 ASSUMPTIONS EDIT-001 追記。多場次結構不必搬：建立流程的多場次發布後即拆成系列（SER-001），詳情頁一筆＝一場的模型不變 **2026-08-11 同日第二輪（使用者指示）**：詳情頁「票種」分頁改名「票務」；新增卡片／列表雙檢視——卡片＝就地編輯（同建立流程一票一卡邏輯）、列表＝營運視角（縮圖＋名稱／庫存／保留中／銷量／票價／手續費／狀態，庫存＝張數−銷量−保留中）；列表每列可暫停販售／重新販售（翻 `t.paused`，營運動作不需先按編輯）；票務分頁底部新增**票務商品**唯讀清單（捆綁包，BDL-001 編輯待上游）；store 種子補 `reserved`／`paused`／`bundles`（高雄＋台北示範）。**保留中／暫停販售是上游規格沒有的營運概念**（F5 只有 Paused 一句），欄位定義待上游 **2026-08-13（使用者指示）**：工作列 split-button 主鈕與空狀態 CTA 由「＋ 新增活動」改「＋ 建立活動」，與下拉選項、空狀態說明文字用詞對齊，純文案不動結構 **2026-08-13 活動詳情補上生命週期動線（使用者指示「依用戶旅程檢查、全部修」，D188）**：`event-detail.html` 頁首補三顆依階段互斥的動作（發布並開賣／取消活動／再辦一場），開賣前擋資料缺口（名稱／場次日期／場地／票種，**不拿起飛前檢核清單當阻擋條件**）、取消先列影響範圍（退幾張、多少錢）；新增 `cancelled` 階段畫面（徽章、分頁子集、結案卡改寫）；總覽新增結案卡（最終到場／未到場／淨收入／結算＋匯出與再辦一場）與線上房間卡（共看派對，D149 的暫代落點）；售票中改場地或場次日期時間，儲存前先開「會通知 N 位持票人」的確認彈窗；起飛前檢核清單自退款分頁搬到總覽（Dashboard F4 CTA 同步改 `#overview`）並只在開演前的三個階段出現；發布設定在草稿／已排程改為可編輯；通知三個開關接線並新增寄送紀錄卡；KPI 隨階段收、退款佇列 KPI 改講「v1 停用」、草稿空值有退路、系列連結一律帶 `?id=` 且只在有母頁時給連結。`events-store.js` 新增 `setStage`／`resetStage`／`checkinSummary`、`series.hasPage`、`room`，`realive-world-tour` 三場補 `series.id`，`taipei-nye` 補票種。**與 §7.2「僅 Draft → Cancelled 可逆」有出入**（原型把取消開放到已排程與售票中），見 ASSUMPTIONS STAGE-001／WRAP-001／WP-001／PUB-001 **2026-08-13 同日（B 反饋「找不到組合包資訊」）**：票務分頁的票務商品卡改常駐（無組合包時給空狀態，共看派對仍收起）＋卡頭數量徽章；總覽的活動詳情列補票種與組合包兩列摘要＋「管理票務與組合包 →」跳分頁；`events-store.js` 的 `album-signing-taipei` 補一組組合包示範。組合包的編輯能力仍待上游（BDL-001） |
| 5.1.6.1 Create Event | `create-event.html` | 已實作五階段；2026-06-15（D060）補即時預覽（§5.2.5）、自動儲存＋狀態（§5.2.4）、QC 五項發布硬閘＋＊inline 驗證、Review 回填、上傳 demo；~~單票種子表單欄位待補（PG-009）~~ **2026-08-04 已補**：步驟 4 原本只有一顆把計數 +1 的虛線卡、沒有任何可填欄位，此輪照 F9／F9.1／F9.2 建成 `ds-components/ticket-tier-card.css`——票種卡兩態（收合＝卡標＋唯讀價格數量＋Edit＋⋮ 複製／移除；編輯＝名稱／價格／數量三欄＋每欄清空 ✕＋卡底 Remove／Cancel／Save）、驗證（名稱 1–40 字不重複／價格整數 ≥ 0／數量整數 ≥ 1）、Σ 數量 ≤ 容量（inline 轉紅＋發布前擋下並跳回步驟 4）。entitlement 仍屬 5.1.6 F5，本流程不收；容量校驗的餘留口徑見 ASSUMPTIONS CAP-001。**2026-08-04 同日第二輪（呈現層，來源 Beamco Artist Portal v3 的票價設定設計）**：票種卡改網格版型（不做橫向捲的 List View，使用者裁決）、卡面重排（⋮ 留標題列、Edit 改卡底整寬鈕、價格與數量改標籤靠左值靠右）、新增選配的批次編輯。該設計的其餘四塊——活動總支出／每人限購、營收試算（平台費與 Artist profit）、折扣與忠誠點數、Quick Create v1——**在 5.1.6.1 沒有對應欄位，屬產品變更提案**，見 ASSUMPTIONS TIX-001。**同日第三輪已落地其中三塊（仍屬提案、未回寫 documents）**：購買條件（不限／限時間／限粉絲分級）、每人限購、折扣（％＋同一組條件），範圍走繼承＋覆寫（活動層預設＋票種卡自訂）；收入試算四列（預估營收／平台費 5%／活動支出／你實拿），營收用折後價保守估、實拿為負照實顯示。**仍未做**：Quick Create v1、Beamco 的 List View。**2026-08-04 第四輪**：新增第 5 步「票券銷售」（票券設計＋套組銷售），**與本規格 §4「5 階段」直接衝突、原型已成 6 步**，未回寫 documents，見 ASSUMPTIONS TIX-002；票券設計對線上活動收起、共看派對整步跳過；套組掛既有共用模組 `bundle-editor.js`，「一定含一張票」的票種勾選已接上（共用模組加 `work`／`cover`／`tickets` 三個選配旗標，募資兩個掛載點不受影響）。**同日第五輪**：類型改為閘門、不再佔一個步驟（stepper 五格，**與本規格 §4 五階段的第一階段「Type」衝突**，見 ASSUMPTIONS TYPE-001）；細節新增類型快選與「從 bookyay 帶入」搜尋框（只有搜尋框、無帶入行為，使用者明示為暫時變通做法，見 BKY-001）。**2026-07-24（D149，§4.7）**：新增第 6 型「共看派對（Watch Party）」＋全站首個 type 分支——選它後 step 2–4 換共看派對欄位（選影片／房間名稱／時間／人數上限／可觀看地區／隱私／入場券），Review 換 7 項檢核；type-driven 顯隱（`data-show-type`／`data-hide-type`＋`applyTypeVisibility`），重用 segmented／amount-field／select／films-store，無新元件。影片來源／人數上限數字／票價區間／收入分類待上游（UIA-087）。**2026-07-31 呈現追蹤**：`create-event.html`／`edit-event.html` 的圖片上傳槽（縮圖／海報／橫幅／相簿）改用共用元件＋單一直式 750×930（見 STYLE-DECISIONS Q39、ASSUMPTIONS IMG-001）。**2026-07-31（同日第二輪，依 D164／§7.10）**：F4 圖片槽由 4 格併為**主視覺＋橫幅＋圖庫** 3 格（必填數 4→3），槽名不再描述形狀；橫幅維持橫式 `--16x9`／1920×1080（全站唯一保留的橫式圖片槽），是活動保留橫式的例外依據。編輯活動（5.1.6.2 F4）具名槽同步收斂為 2 格＋圖庫列，計數文案 n/4→n/3。IMG-001／PG-023 已由上游採納並結案。**2026-08-06（第六輪，使用者逐項圈選）**：語言欄說明撤除；場地新增「進階」收合（集合地點＋交通，兩欄選填短文字，走既有 `.field-more`）；「容量」改成「活動人數」並拆成最少（選填）／最多（必填）兩欄，最多沿用 `data-ce="capacity"` 當票種加總上限。**這三項在 5.1.6.1 沒有對應欄位，屬產品變更提案**（集合地點／交通／最少人數是新增資料欄，容量的單值語意變成區間），未回寫 documents，見 ASSUMPTIONS ATT-001。同輪純呈現層調整兩項：票種網格固定三欄、票種卡的自訂規則開關由卡內 switch 列改成卡標同一行的按鈕。`edit-event.html` 仍是單一「容量」欄、無場地進階兩欄，欄位集分歧見 ASSUMPTIONS GEO-001 ⑤。**2026-08-06（第七輪，系列活動）**：步驟 3 新增「系列場次」開關與場次清單（第 1 場＝主欄位鏡像，第 2 場起各自可改日期與場地，新增時預設帶入主欄位值），票種數量**每場獨立**（收入試算乘場次數、新增「場次」一列），Review 新增「場次」摘要列；活動清單（5.1.6 F1）新增系列母列＋可展開子列（`product-list.css` 的 `__row--group`／`--child`）。**規格既有的系列是平的、且沒有建立入口**（F5／5.1.6.3 §2.12 只定義每一場身上的系列註記與詳情頁的系列分頁；5.1.6.1 完全沒提系列），本輪等於補上建立端並把清單改成母子兩層——屬產品變更提案，未回寫 documents，見 ASSUMPTIONS SER-001。**2026-08-06 同日第三輪**：使用者裁決系列要有自己的詳情編輯頁，因此新增 **`series-detail.html`（系列母詳情頁）**——共用設定改一次同步所有場次、場次分頁列出各場並可進該場詳情；三種活動詳情頁自此成立（單場／系列母／系列子）。**`0-設計規格書.md` §3.2 產品地圖尚未有這一頁**，依「頁面清單的唯一來源」規則應先補 §3.2，本輪因使用者當次明確指示先做在 site，見 SER-001 ⑪。同輪裁決另三項：各場名稱先與系列同名（SER-001 ⑥）、已開賣改動要擋或通知但原型只到「說出來」（⑦）、取消系列中的一場沿用單場活動的退款規則（⑧）。**2026-08-06 第四輪（編輯搬進詳情頁）**：`edit-event.html` 撤除（整頁換成導引墓碑、全站入口改指詳情頁），編輯改在 `event-detail.html` 的總覽與票種兩個分頁就地進行；系列母頁維持「儲存並同步」，系列子場不給編輯入口。**§3.2 產品地圖與 `5.1.6.2-編輯活動.md` 仍以獨立頁描述，屬上游待改**，見 ASSUMPTIONS EDIT-001。同輪新增建立流程的「售票方式」（自訂門票／不設門票，選後者時票種與票券銷售兩步跳過、檢核移除該項）；免費不做成獨立模式，票價 0 即免費。**`5.1.6.1` 無此欄位與路徑**，見 ASSUMPTIONS TIX-003。**2026-08-06 第五輪**：售票方式移到「場地與時間」的人數之後；細節新增**進階詳細資料**（活動內含物四選、需攜帶物品、活動須知）與**條款與細則**（兩個顯示開關）。**`5.1.6.1` §5 資料清單無這兩組、`documents/` 亦無條款同意規則**，見 ASSUMPTIONS TERM-001。**2026-08-08（demo，不在產品頁上）**：依使用者提供的 `docs/活動建立流程修改.md` 另做 `docs/create-event-demo.html`——步驟重排成四格（基本資料／場次與票務／發布設定／確認）、入場方式提到票務段最前、票種與場次併成一步、新增「其他活動設定」（三個限購）與「發布設定」（開賣與停售時間、取票方式三選、公開或私人＋連結與 QR），票種卡補手續費／標籤／加入商品／拖動排序，並實作「從 bookyay 帶入後 `(fix)` 欄位鎖定」。**`create-event.html` 未動**；本規格 §4 的階段定義與這份 demo 的四步差距、以及 bookyay 整合本身，見 ASSUMPTIONS BKY-002。**同日第二輪（demo）**：依使用者指示把「場次與票務設定」改成容器結構——場地共用一份、日期與起訖與入場時間搬進場次列（每場一組），系列場次開關撤除、清單永遠存在，門票名稱＝`{場次名＋票種}`（票種卡即時顯示會產生的票名，另有選填的特殊票名覆寫），票券設計先由發布設定搬回票種之後、隨即依使用者裁示整段撤除（文件未提，這一輪不做）。**同日第三輪**：場次與票務再拆成兩步（共五步：基本資料／場次／票務／發布設定／確認），票種改成「活動層定義一次、逐場列出並可改張數或整種關掉」，收入試算逐場加總；「其他活動設定」區塊撤除，三個限購併進購票規則彈窗、取代原本的「每人限購」單欄。免票入場改回跳整步（票務）。**同日第四輪（依使用者指正）**：模型改成**門票＝場次 × 票種的矩陣**——價格／張數／手續費／標籤／加購／規則掛在交叉出來的每一格，票務那一步以場次為主軸分組列出全部門票；第三輪加入的「同一套／各自設定」切換與「每一場開哪些票」小表同日撤除。「每一場的人數」改為「活動總人數」搬到基本資料，容量比對改成全部場次門票張數的總和。**同日第六輪**：票種從票務拉出來獨立成第 4 步（共六步），只定義名稱／販售標籤／預設價格／預設張數；加一種就在每一場各生一張門票、刪一種一起收，票務那一步因此只剩逐格填。預設值是種子（未被逐格改過的門票跟著走，改過即脫鉤），不是共用連動。場次清單另立頁內 `.ce-sess` 樣式、未動共用的 `session-list.css`（`create-event.html` 仍消費原元件）。**2026-08-09（demo，第七至十三輪）**：票務的編輯由卡內展開改成彈窗（分頁最終收斂成「設定」與「購買條件」兩個，設定含門票名稱／總價／票價／張數／手續費／加入商品）；總價定義為「所有設定跑完的最終價」＝票價＋加購商品再套折扣，與收入試算共用同一個算法；新增分票（從原本那張切出張數，原本至少留 1 張）；卡標改顯示門票名稱、bookyay 票多一列「票務組合名稱」；**bookyay 帶入自成流程第二關**（選活動類型 → 要不要帶入 → 正式流程），基本資料那一步只留帶入態說明與取消帶入。以上皆屬 demo、`documents/` 未回寫，見 ASSUMPTIONS BKY-002 ⑩–⑱。**同日第十四輪**：補回 D149 的共看派對——demo 首版漏搬，本輪把類型卡與四段分支區塊照 `create-event.html` 原樣搬回，共看派對不經 bookyay 那一關、票務與發布設定兩步跳過（跳過發布設定見 BKY-002 ⑱）。同輪修 `ds-components/review-row.css`：補 `.review-row[hidden] { display:none; }`，`create-event.html` 選共看派對時 Review 兩份摘要並排的問題一併解決（這一項有動到 `create-event.html` 消費的共用元件，頁面本身未改）。**同日第十五輪**：票務的每一張門票加上圖片欄（卡面方形縮圖＋彈窗設定分頁的直式上傳格），留空＝跟著活動主視覺走。**`5.1.6.1` 無此欄位**，見 ASSUMPTIONS BKY-002 ⑲。**同日第十六輪**：bookyay 帶入那一關的結果清單改用活動類型閘門的卡片樣式（已選＝該卡 hover 的樣子）、返回鍵移到標題左邊、兩顆出口改名「略過」「帶入」；基本資料的帶入狀態區塊（說明橫幅＋取消帶入）撤除，**取消帶入的出口因此不存在**，見 ASSUMPTIONS BKY-002 ⑳。**同日第十八輪**：票種由獨立步驟改成票務裡的「快速設定」彈窗（上半定義票種、下半列出矩陣結果並標示跟著預設／已自訂，可單列或整批改回跟著預設），流程六步變五步（第 4 步保留給共看派對的入場券、一般活動跳過）；補回「手動新增門票」，被刪掉的組合不再自動長回來。bookyay 帶入時該彈窗唯讀。見 ASSUMPTIONS BKY-002 ㉑。**同日第十九輪**：手動新增門票改成坐在每一場門票後面的虛線新增格（沿用 `.tier-add`），場次由格子決定、彈窗只問票種；**同日第二十輪**：bookyay 帶入時不給這個新增格（票務組合由 bookyay 決定，要多一種賣法走分票，使用者裁決）；**同日第二十一輪**：手動新增的票可以就地新建票種（下一張票即可挑它），新票種以該張票的值當預設與種子，且只屬於當下那一場。**2026-08-09 正式化（使用者指示「將 demo 做到正式的」）**：demo 升為正式頁、取代原本的 `create-event.html`（舊流程完整備份成 `create-event-legacy.html`，入口＝活動頁建立按鈕的 split-button 下拉第二項）。**本規格 §4 的五階段定義與正式頁現在的五步（基本資料／場次／票務／發布設定／確認）名稱與內容都對不上**，且 BKY-002 底下所有未經上游核准的變更自此上線（先前 `deploy.sh` 排除 `docs/`、只在本機可見）；2026-08-08 撤除的「票券銷售」步驟（TIX-002）也隨之從線上消失。見 ASSUMPTIONS BKY-003。**同日收斂（使用者裁示「樣式一律以既有 DS 為主」）**：頁內自造的搜尋挑選器、場次清單、單選鈕三組退場，改用既有 `combobox`／`session-list`／`radio-list`（已加入的商品改用可移除 chip）；`session-list` 為此新增欄位標題列與多欄變體。裁決理由見 STYLE-DECISIONS Q55／Q56／Q57。**同日再五項（使用者逐項圈選）**：門票圖片自成一列；**門票名稱的預設由「場次名＋票種」改成票種名稱**，卡面「票務組合名稱」那一列撤除（門票一律坐在場次分組底下，名字裡再寫一次是重複）；門票彈窗分左右欄、圖片移到右欄；**購買條件不再折疊**，規則一律攤開、動了才複製一份給該張票（copy-on-write），脫鉤後才給「改回跟隨活動預設」；全站 disabled 欄位統一成透明底一種（見 STYLE-DECISIONS Q58）。**同日六項（使用者逐項圈選，分三輪）**：票種與購票規則合併成單一「票務設定」上層設定、門票卡補限購與折扣摘要；販售方式加「發布後直接開賣」開關（兩階，帶入時鎖住）並補上漏接的必填檢核；取票方式三卡等高、鎖定態改用顏色；上傳格的綠色已填框全站撤除（見 STYLE-DECISIONS Q59）；**bookyay 帶入的圖片改成可編輯可新增**；**表演陣容確認 bookyay 沒有這項資料**，改為自行維護並補上搜尋加入的流程（原本那顆新增按鈕沒有任何事件處理器）。表演陣容與圖片是否為 bookyay 帶入欄位，原記於 ASSUMPTIONS BKY-002 ⑮ 待確認，本輪由使用者裁定：陣容不是、圖片是但不鎖**2026-08-10 呈現追蹤（使用者指示，兩項）**：bookyay 帶入閘門的結果清單由常駐清單改成 `owner-lookup` 下拉（聚焦即整份列出、打字才過濾、點選填回搜尋框才解鎖「帶入」），連帶撤除 `.source-gate__empty`、`.radio-cards--list` 轉為退場候選；分段進度條的步驟名改成等寬格線、與軌道分段一一對齊，「正在走的那一段」由 `--primary` 稀釋色改中性灰（深底上會混成泥巴棕、讀起來像沒渲染完）。**皆為呈現層調整**，欄位、必填、步驟集合與帶入資料不變。 **2026-08-10 呈現追蹤（第四輪，使用者逐項圈選）**：只有一場時票務外框撤除（`.ce-tiergroup` 只在多場時才掛外框，同組標題判斷一致）；販售方式／取票方式選項卡與下方揭示欄位補 16px 間距；分段進度條退回第一步時的橘色殘影修掉（填充停止寬度動畫）；bookyay 閘門的返回鍵改對齊標題本身而非含副標的整塊。**皆為呈現層調整**，欄位、必填與步驟集合不變。 **2026-08-10 呈現追蹤（第五輪，使用者指示）**：進度條每一步都可自由點開（點擊條件放寬成「除被跳過的步驟外皆可跳」，元件層以 `--anystep` 修飾類同步打開可點外觀）；分段進度條的 hover 範圍改成剛好覆蓋所屬分段。**皆為呈現層調整**，發布前的必填把關仍在 Review 的檢核清單上，欄位與必填條件不變。 **2026-08-10 活動形式「定點／巡迴」（第六輪，使用者指示；產品變更提案 TOUR-001）**：場次那一步最前面新增二選一，定點＝原本唯一的模型（場地整場共用一份）、巡迴＝一站一盒（站名／日期／時間／場地名稱＊／地址／集合地點／交通各自設定）；發布前檢核的「場地」在巡迴時要求每一站都填；定點切巡迴把共用場地複製進第 1 站；bookyay 帶入鎖定為定點。**上游規格把場地寫成整場共用一份（「無論哪個場次，地點都一樣」），本項推翻該前提**，欄位歸屬、買家端呈現與每站是否各自有人數上限／售票期間待上游裁決，見 ASSUMPTIONS TOUR-001。 **2026-08-10 呈現追蹤（第七輪，使用者指示）**：一張票都還沒有時點「新增第一張門票」，先出現選擇彈窗問要用快速設定（先設票種與購票規則、每一場自動各生一張門票）還是自己一張一張加（沿用原本的單張新增流程）；判斷條件為「目前一張票都沒有」，兩條路走完都會生出票，因此自然只問第一次。**純呈現層**，欄位、必填與票務矩陣模型不變。 **2026-08-10 撤除三組欄位（第八輪，使用者逐項圈選）**：①「活動總人數」（最少／最多）整組撤除，連帶超賣硬閘、剩餘張數提示、超量紅字三處失效（記 ASSUMPTIONS ATT-002）；②「活動形式」（定點／巡迴）整組撤除，只有定點活動，**場地回到整場共用一份＝本規格原本的模型**（ASSUMPTIONS TOUR-001 已標記撤回）；③「入場方式」（購票入場／免票入場）整組撤除，「至少 1 種票種」由條件式改為恆為硬閘（記 ASSUMPTIONS SALES-001）。**①③ 與本規格不一致**：最多人數的必填、票種張數不得超過總人數、免票入場三項在原型中已不存在，待上游裁決。 **2026-08-11 票務設定彈窗改版（使用者要求重新規劃版面）**：由三段直向堆疊改成左右分欄（左＝票種＋購票規則、右＝生成的門票且 sticky），彈窗寬度 880→1180px；購票規則預設收合成一行摘要（只收這一側，票種彈窗的購買條件分頁維持原樣）；「全部改回跟著預設」由 footer 搬到結果區標題列、footer 只留「完成」；結果標題帶張數；票種區副標只在已有票種時出現。**純呈現層**，欄位、規則模型與矩陣邏輯不變。 **2026-08-11 補回「多場次」開關（使用者指示）**：場次區標題列補回單場／多場開關（原 2026-08-06 的「系列場次」開關，在 bookyay demo 升正式頁那一輪隨整頁被換掉，屬搬遷附帶損失）。關＝單場（一列、無場次名稱欄、不可增減）；開＝多場（場次名稱欄出現、可增可減，開啟時自動補到兩場）。`multi()` 改由開關決定而非以場次筆數反推。bookyay 帶入依資料自動開關並鎖定。**純呈現層**，場次資料模型與票務矩陣邏輯不變。 **2026-08-11 撤除分票＋新增「票務商品」步驟（使用者指示）**：分票（從一張票的張數切出一部分成另一張獨立設定的票）整組撤除；票務之後新增第 6 步「票務商品」，用既有的共用套組編輯器把票種與商品綁成捆綁包。**捆綁包與單賣的票共用同一個數量池**——不另設庫存，賣掉一組就從它含的那張票扣一張（活動變體本來就不渲染數量欄，「販售上限」是自願的子上限）；右軌逐票列出「現在幾張 · 被幾組用到」。步驟重編 6→7、7→8（`nextStep()` 依數字遞增找下一步，編號順序就是流程順序）。**上游規格無「捆綁包」與此步驟**，見 ASSUMPTIONS TIX-002／BDL-001。 **2026-08-11 票務分兩種（使用者裁示）**：①**票種階層票**——手動新增時打的名字對上既有票種，就掛進那個票種、跟著預設值走並出現在矩陣裡；②**獨立票**——對不上就不建票種，`type` 留空、名字歸這張票自己，不被矩陣複製到其他場次、票種預設值也動不到它。`syncMatrix` 的孤兒清理同步放行沒有票種的票。門票卡與票務設定清單各標一個「獨立票」記號。**上游規格只有票種階層一種**，獨立票為產品變更提案，見 ASSUMPTIONS SOLO-001。 **2026-08-11（第二輪）**：①「活動形式」（定點／巡迴）依使用者「刪錯了」指示整組加回，同批撤除的活動總人數與免票入場維持撤除；②手動新增票券改成明講的二選一「以票種建立／以獨立票建立」，不再靠名字對不對得上隱含判斷。 **2026-08-11 撤除「多個場次」開關（使用者指示）**：場次數不需先宣告，「新增場次」一律可用（bookyay 帶入時鎖住）；`multi()` 回到由實際場次數推導，場次名稱欄、門票名稱帶場次、票務分組標題等一律跟著實際場次數走。**純呈現層**。 **2026-08-11 票種欄改「選擇票種」（使用者指示）**：下拉一律列出所有票種並顯示其預設值，無相符時改顯示說明而非收起；選了票種一律帶入其預設值（此前只填空欄）；票種建立時以當下數值為種子，之後改動即為複寫狀態（沿用既有 `_sp/_sq/_sf` 機制）。**純呈現層**。 **2026-08-11「這張票怎麼建」改樣式與命名（使用者指示，對照活動列表頁分類膠囊）**：視覺由 `.segmented` 改為 `.filter-tabs`（已選＝品牌橘 tint），選項改名「選擇票種」→「以票種建立」、「建立獨立票」→「獨立票」；角色仍是二選一設定（`role="radiogroup"`），只借視覺不借語意。**純呈現層**。 **2026-08-11 門票編輯彈窗撤除四處冗餘說明與重複欄位（使用者逐項圈選指示）**：「這張票怎麼建」標題、獨立票模式的常駐說明句、門票圖片「活動還沒有主視覺」說明句撤除（皆為與旁邊欄位重複的文字，純呈現層）；「加入商品」欄位整組撤除（把商品綁進票的動作改由「票務商品」步驟的捆綁包統一處理，資料層與票卡顯示未動，見 ASSUMPTIONS BDL-001）。 **2026-08-11 票種各自的提前入場時間＋場次的早到處理政策（使用者提供截圖指出需求，產品變更提案 EARLY-001）**：票種彈窗新增「提前入場（分鐘）」選填欄位，跟著場次「開放入場」時間換算，常駐說明即時顯示分鐘數與換算後的實際時間（不硬記時間差）；票卡補一列摘要（只在真的設了才出現）；場次那一步新增「早到怎麼處理」二選一（提示職員／直接擋下），使用者裁示這是場次層的單一政策、跟票種無關，一個活動只設一份。**上游規格報到規則（F5）沒有票種各自的入場時間，也沒有早到處理**，見 ASSUMPTIONS EARLY-001。 **2026-08-13 bookyay 帶入兩項（使用者指示）**：①帶入閘門的搜尋結果新增「已匯入」態——已經帶進 ztor 的場次照樣列出但整列 disable、右掛「已匯入」徽章，刻意不濾掉（搜尋的目的常是確認自己建過了沒，濾掉會回答成「找不到相符的活動」）；這回答了 ASSUMPTIONS BKY-002 ⑰ 的「同一個 bookyay 活動被帶入過兩次要不要擋」。②帶入後的落點由第 2 步改成第 6 步「票務商品」，前四步整份鎖住、進度列標為已完成但仍可回頭點進去。**2026-08-13 票務商品那一步重新設計落地（使用者裁示「先照這樣改正式」，先有 `docs/bundle-step-demo.html` 探索）**：捆綁包卡改成三段、順序倒過來（內容 → 定價 → 包裝），名稱移到最後並自動擬建議名；適用票種依場次分組＋跨場次票種捷徑＋選完收合成 chip（三場兩票種時卡高由 1,464px 降到約 1,065px，且不再隨場次數膨脹）；空狀態新增「這個活動不做捆綁包」正式出口；右軌由「票的數量池」改成只列被綁到的票；**套組原價開始計入票價**（取勾選的票裡最貴的那一張）。新增「一組通用所有場次／每場各一組」二選一，後者一次展開成 N 組。**上游規格沒有「每場各一組」這個概念、折扣的計價基準（多選票時以哪一張為準）也未定**，見 ASSUMPTIONS BDL-002。實作走共用套組編輯器的 `layout:'sections'` 版型，募資兩個消費頁不受影響。 **ztor 怎麼判定一場 bookyay 活動已被帶入（原型是假資料寫死）、已匯入那一列要不要能點去看已建好的活動、以及「已完成」代表 bookyay 資料就位還是創作者確認過**，見 ASSUMPTIONS BKY-002 ㉒。 |
| 5.1.7 Fans（含 F7 Fan Detail、F8 Tier Settings） | `fans-crm.html` | 2026-06-15 補 Leaderboard\|Hall of fame tab、View explanations、F3 Pareto 自動洞察、F5 列級訊息/詳情入口＋Recovered、F6 即時過濾＋Load more、F4 條件橫幅；群發訊息（5.1.7.1 v2 / D058）已建為 modal；F7 粉絲詳情 2026-06-15 新建 fan-detail.html（行為時間線/消費/活動參與/標籤＋名人堂 peak tier）、F8 分級與權益設定新建 tier-settings.html（雙閘門檻/行為加權/Benefits 編輯/規則版本），由 fans-crm 列詳情與 Tier settings 入口進入；雙閘驗證與版本邏輯標待確認（上游）。2026-08-01（使用者裁決）：分級設定由分頁改成頁首按鈕＋彈窗（`.payout-dialog--xwide` 1180px），彈窗內再分三個分頁——門檻與權益／行為加權／規則；卡頭標題移除（分頁名已交代），說明句上移到卡頭。行為加權與規則兩張卡原本硬編中文，已補 12 個 i18n key，英文語系不再夾中文 |
| 5.1.7.8 Fan analytics · incl. external（粉絲分析：含外部） | `fan-analytics.html`、`ds-components/finding-card.css`、`ds-components/source-status.css` | 2026-07-31 新建（spec 5.1.7.8 / D158–D160）：受眾洞察由「無獨立頁的全域能力」升為 Fans 群組第 6 個目的地。F1 平台連結與同步狀態＝頁首操作區的常駐膠囊（不占版位，異常轉 destructive，明細與整合設定入口收在展開面板內）；F7 本月重點摘要置頂（結論＋可掃視的支撐數字＋依據＋追查入口，新元件 `.finding`）；F2 受眾總覽、F3 多維影響力（四維並列、規格禁止合併成單一分數）、F4 受眾分群（年齡維度規格保留但不呈現，資料來源上游未定義）、F5 轉換漏斗、F6 商業價值（口徑引用 Earnings §7.3）、F8 建議行動（本頁唯一給動作的區塊，每則附依據且可關閉）。未連結態整頁只剩來源清單，不以零或估算填版面。Dashboard F7 受眾欄補「進入受眾分析」入口，收掉 D057 的懸空 deep-link。第一批接入平台、同步頻率、跨平台去重規則、年齡資料來源、期間選項皆為上游待確認（主規格 §8.17）。2026-08-01 改雙軌（D165）：F1 更名「資料來源與涵蓋範圍」，來源分官方串接（Spotify、StreetVoice，ztor 端接好、創作者無從授權）與自行上傳（YouTube、Instagram、TikTok，ztor 未串接）兩組，Spotify 排第一；全站移除「連結／重新連結」；上傳資料檔在本頁就地完成（`partials/source-upload-modal.js`，殼層 Q27 `.payout-dialog` ＋ `.upload-tile--file`）；官方串接異常顯示「ztor 處理中」，Spotify 給上傳備援、StreetVoice 不給動作。檔案格式、同期間重複上傳的處理、上傳資料的效期三項上游未定（ASSUMPTIONS PG-026～028），彈窗文案因此不寫任何格式或欄位名。**2026-08-06（B 反饋）**：「ztor 粉絲組成」卡依使用者裁示搬到 5.1.1 儀表板，本頁不再呈現站內分級組成；作品榜改整寬區段，F3–F6 維持兩欄。⚠ 站內／跨平台兩個分母的對比在本頁不再有畫面承載，待使用者裁決是否補回。|**2026-08-09（D181）改名並封存**：本頁改名「粉絲分析：含外部」，因為它依賴的 Spotify／StreetVoice／YouTube／Instagram／TikTok 目前多數不回傳資料給 ztor。頁面與規格 F1–F8 完整保留、內容未縮水，麵包屑之後加一條封存說明條並導向新頁；側邊欄由第一位移到 Fans 群組最後。使用者原話：「只是想先備份起來，因為有許多平台資料得不到，所以先做 ztor 可以拿到的資料畫面」。
| 5.1.7.9 Fan analytics（粉絲分析） | `audience-report.html`、`js/audience-store.js` | **2026-08-09 新建（spec 5.1.7.9 / D181）**：只用 ztor 現在真的拿得到的資料，分影視／音樂／貼文三個分頁，參考來源是 ztor 自己的績效報告站（唯讀觀察，非產品規則來源）。**全頁零金額**——參考站每張卡的數值都是版稅，使用者裁示去掉版稅，因此改成次數與人數兩條軸；帶貨收益移到收入管理（見 5.1.8）。影視／音樂的次數（總次數、Top 10 作品、地區、平台）全部轉引 `js/performance-store.js`，不留第二份副本；作品排行依 persona 過濾（`roy.work1–11` 的 video／music 對照同 earnings-sony），音樂用該檔的十首歌。受眾屬性四區（性別／星座／MBTI／生肖）**只能用人數**且必須揭露涵蓋率——這些屬性只存在於 ztor 會員資料，外部平台回傳的彙總數字推不出任何一個人的星座。貼文分頁的 KPI、型別表、Top 10 全部由明細列推導，切期間時五組數字一起動、加得回來。新增 `.perf-rank--wide`（名稱欄 148px→340px，貼文標題是一整句）。**待確認**：貼文與內容租借在 `documents/` 與 `requirement/` 全庫零記載、星座與 MBTI 的資料來源、涵蓋率口徑、匯出格式、期間預設值——見主規格 §8.20 十條。
| 5.1.8 Earnings | `earnings.html` | **七分頁**（總覽／版稅／交易明細／項目收益／提款／稅務文件／如何運作）。**2026-07-31 新增「版稅」分頁**：總版稅＋期間切換＋資料截至·延遲說明＋依來源拆解（發行商報表·站外／Ztor IP 版稅·可結算／Ztor 授權·可結算／補登·未驗證）＋作品 Top（依版稅金額｜依串流下載次數）＋地區表現＋串流平台表現；資料點對照 cocreate 提案版稅頁，外部只收版稅、不含站外商品販售。KPI 改為總收入／Ztor 收入／站外版稅／可提領（互斥可加總），淨利移入項目收益、待結算併進可提領卡。曾拆成 `earnings-overview.html`＋`earnings-ztor.html` 兩頁，已併回；兩支檔暫留磁碟不掛導覽。規格未回寫，見 ASSUMPTIONS ESP-001～007。Breakdown 用「本期間(F12)/依項目(F11)」segmented；F8 淨利池/退款準備金在 Payouts、F10 補登 popup 改由版稅分頁觸發、F7 逐筆可追溯（費率版本/Event ID/展開金流瀑布）——皆已覆蓋；多地稅務仍待收斂 |**2026-08-09（D181）新增第八型收益「貼文帶貨收益」**：使用者裁示「在 earning 中也做貼文，帶貨收益就放在這裡」——貼文的成效（瀏覽、互動、內容租借次數）留在粉絲分析頁，金額只在收入管理出現一次。`partials/finance-overview.js` 的 `TYPES`／`TYPE_SERIES` 加 `affiliate`，掛在 film／music 兩個 family 上（貼文談的是自己的影音作品）；新增 `--chart-8` 赭色 token（1–7 已佔滿橘黃綠藍紫桃青）；收益說明新增條目並帶 `id="affiliate"` 供粉絲分析頁的連結落點。**待確認**：分潤比例、結算週期、與平台費率的關係、內容租借的定價權——全庫零記載，見 5.1.8 F5 與主規格 §7.3。
| 5.1.8.1 Request Payout | `earnings.html`、`request-payout.html` | popup 主畫面/新增銀行/結果＋不可逆確認閘門（§4.5）＋摘要含結算來源/費率版本（§4.4）已覆蓋；非台灣地區欄位與費率仍待上游（PG-004）|
| 5.1.8.2 Manual Entry | `earnings.html`、`partials/manual-entry-modal.js` | 由 F7 觸發的補登 popup（重用 canonical dialog 外殼）：交易項目/分類/日期/金額/幣別/備註/附件＋未驗證·不計入可提領/提款/稅務提示已覆蓋；補登審核、附件規則、未來日期等仍待上游 |
| 5.1.9 Settings | `settings.html` | 2026-06-15 通知改事件×管道矩陣（含 In-App、payout/KYC/compliance Email 鎖定）、勿擾、寄測試、Active Sessions、未存離開提示、稅務 Edit、整合補 Stripe/GA 與失敗一致呈現；刪除帳戶文案改 soft-delete（D053）；**F7 合規（唯讀）已落地**（KYC 狀態/用量上限/凍結/地區限制）。KYC/2FA/收款方式狀態機/Webhooks **流程**待 5.1.9.x 子流程規格（PG-007）；團隊權限仍受上游待決限制。**2026-07-03（D108／D110）**：語言固定繁中，顯示模式保留可切換、預設側邊欄（settings 顯示模式兩卡保留；`data-nav-toggle` 保留）。**2026-07-14 覆寫 D108 的主題限制**：`theme.js` 恢復 light / dark / system 偏好；合法 localStorage 偏好跨頁保存、無值預設 dark，`?theme=` 優先但不寫入偏好。Settings 不新增主題卡，`section-test.html` 提供外框與主題驗證頁（UIA-048） |

## 更新順序

1. 先更新 `requirement/`、`documents/decisions.md` 或 `documents/`。
2. 更新本表與 `ASSUMPTIONS.md`。
3. 由 `project-ui-creator` 決定呈現並更新 `BUILD-SPEC.md`。
4. 實作頁面與元件。
5. 更新 `UI-CHANGES.md` 並執行驗證。
