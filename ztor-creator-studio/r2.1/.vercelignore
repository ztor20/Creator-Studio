# 部署到公開網址時排除的東西（2026-07-28）。
# r2.1 這個資料夾同時是「原型網站」與「工作區」——規格稿、盤點筆記、PR 預覽、
# 參考截圖都躺在同一層。整包丟上 Vercel 等於把內部文件公開，而且會被搜尋引擎索引。
# 原型頁面本身要上線，這些不要。

# 內部規格與工作筆記
*.md
# 例外：devtools 的版本 gate 在執行期 fetch 這一份（js/devtools.js:296）。
# 排掉它線上就會 404，cheat code 面板的版本切換會退回預設值——那是 demo 要用的功能。
!feature-scope-map.md
docs/
scratch/
references/

# 尚未收斂的分支實驗
funding-test/

# 設計系統的機器輸出，非頁面資產
ds-baseline.json

# 本機開發用
.DS_Store
Thumbs.db
