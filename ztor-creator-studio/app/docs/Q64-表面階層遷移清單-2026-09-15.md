# Q64 表面階層 token 遷移｜消費者盤點清單

專案：`ztor-creator-studio/site/r2.3/`
依據：`ds-components/_tokens.css` 新增的六支 Role 別名（L0～L3），詳見該檔 69～81 行。
搜尋範圍：`ds-components/*.css`、`shared.css`、`partials/*.js`、`js/*.js`、根目錄 `*.html` 的 `<style>`。

別名對照（全部是純轉指，數值本身沒變）：

| Role 別名 | 現行 Foundation | 亮/暗值 |
|---|---|---|
| `--layer-0-surface` | `var(--ztu-canvas)` | 亮：未調（僅深色 #0a0a09 有意義）|
| `--layer-1-surface` | `var(--ztu-glass-bg)` | 暗 5% 白 |
| `--layer-2-surface` | `var(--ztu-film)` | 暗 8.5% 白 |
| `--layer-2-line` | `var(--nest-line)` | 暗 transparent，亮 8% 黑 |
| `--layer-3-surface` | `transparent` | — |
| `--layer-line` | `var(--border)` | 暗 10% 白，亮 #EAEAEA |

---

## 判定總表

判定欄用詞：
- **遷（零變動）**＝候選 token 是現行值的純別名，換名不換值。
- **遷（有變動）**＝`--nest-surface`（4%）併到 `--layer-2-surface`（8.5%），視覺會變亮。
- **不遷**＝材質/凹槽/控件/分隔線用途，不屬層級系統。
- **不確定**＝證據不足以判斷，留給使用者裁決。

### 第一組：`var(--ztu-canvas)` → 候選 `--layer-0-surface`（共 5 支真消費者，另 3 處為註解文字不算）

| 檔案:行號 | 選擇器 | 現值 | 判定 |
|---|---|---|---|
| `shared.css:250` | `html[data-nav-mode="sidebar"] body` | `background-color: var(--ztu-canvas)` | **遷（零變動）**——這一行本身就是 tokens.css 註解裡「L0 檯面＝body」的原句消費者，改名最乾淨 |
| `ds-components/canvas-stage.css:37-38` | `.canvas-stage__bg-tint`（推斷，漸層層） | `linear-gradient(135deg, var(--canvas-tint, var(--ztu-canvas)) 0%, color-mix(...) 100%)` | **不確定**——是「作品平均色抓不到時的退回色」，用途是材質退回值不是本體層級，語意上改成 layer 別名怪 |
| `ds-components/fan-store.css:162` | `.fan-store__stick::before` | `linear-gradient(color-mix(in srgb, var(--ztu-canvas) 70%, transparent), transparent)` | **不確定**——遮罩/淡出效果的原料色，不是「這塊是 L0 surface」 |
| `ds-components/detail-sheet.css:55` | `--sheet-veil`（自訂變數，供浮層背景用） | `color-mix(in srgb, var(--ztu-canvas) 0%, transparent)` | **不確定**——濃度目前是 0（等於沒有），是暗紗效果的旋鈕，不是層級 surface |
| `ds-components/preview-column.css:103` | `.preview-col--tall > .form-section--outlined > .form-section__head--rule` | `color-mix(in srgb, var(--ztu-canvas) 82%, transparent)` | **不確定**——sticky 表頭底下的模糊遮罩，材質用途 |

### 第二組：`var(--ztu-glass-bg)` → 候選 `--layer-1-surface`（共 48 處，扣掉 6 處實驗/文件頁後 42 支正式消費者）

**判定為 L1（遷，零變動）——29 支，全部是「卡片/卡片等效面」的本體背景：**

| 檔案:行號 | 選擇器 | 判定依據 |
|---|---|---|
| `ds-components/card.css:95` | `.card` | 基準卡本身，tokens.css 註解點名的原型 |
| `ds-components/card.css:15` | `.ztor-card` | 註解：另一種卡型，同配方 |
| `ds-components/admin-ip-bank-table.css:9` | `.admin-table-wrap` | 註解：「補足與 .card 一致的四件套」|
| `ds-components/brand-card.css:30` | `.brand-card` | 卡片本體 |
| `ds-components/alert.css:165` | `.alert--banner` | 註解：「比照 .alert--bar 同一套四件套」|
| `ds-components/alert.css:314` | `.alert--bar` | 註解：「與 .card 逐字相同…不再是站上第二種卡面」|
| `ds-components/canvas-home.css:196` | `.canvas-start__card` | 卡片本體 |
| `ds-components/event-preview-card.css:26` | `.event-preview-card` | 註解：「同一種卡」|
| `ds-components/detail-sheet.css:90` | `.detail-sheet__panel`（面板本體） | 整片浮層的主體玻璃面 |
| `ds-components/fans-guide.css:176` | （guide 卡） | 註解：「配方與 .card 同源」|
| `ds-components/funding-panel.css:120` | `.funding-panel--card`（獨立卡態） | 註解：「配方與 .card 同源」|
| `ds-components/kpi.css:37` | `.kpi` | 註解：「與 .card 逐字相同」|
| `ds-components/media-vault.css:295` | vault 卡 | 註解：「配方與 .card 同源」 |
| `ds-components/media-vault.css:318` | vault 卡 | 同上 |
| `ds-components/media-vault.css:562` | vault 卡 | 同上 |
| `ds-components/media-vault.css:669` | `.vault-tracks` | 同上 |
| `ds-components/media-vault.css:1113` | `.vault-ovcard` | 同上 |
| `ds-components/form-section.css:68` | `.form-section.form-section--outlined` | 註解：「內容區塊…吃玻璃」|
| `ds-components/notification-matrix.css:40` | matrix 容器 | 註解：「同一種卡」|
| `ds-components/post-composer.css:44` | `.post-composer` | 註解：「跟站上的卡同一個面」|
| `ds-components/readiness.css:21` | `.readiness` | 註解：「與 .card 逐字相同」|
| `ds-components/preview-card.css:22` | `.preview-card` | 註解：「與 .card 逐字相同」|
| `ds-components/selection-card.css:43` | `.selection-card` | 卡片本體 |
| `ds-components/selection-card.css:63` | `.selection-card--active` | 選中態沿用同配方（非 hover）|
| `ds-components/store-settings.css:196` | `.ss-fan__card` | 命名即卡片 |
| `ds-components/tabs.css:218` | `.tabs--card` | 命名即卡片 |
| `ds-components/vault-share.css:189` | vault 分享卡 | 註解：「配方與 .card 同源」|
| `shared.css:1557` | `.ip-hero` | 卡片型 hero 區塊 |
| `shared.css:2130` | `.project-card` | 卡片本體 |

**判定為材質（不遷）——10 支，符合任務給的排除例（頂欄／側欄／浮起 hover／按鈕 hover）：**

| 檔案:行號 | 選擇器 | 理由 |
|---|---|---|
| `ds-components/header.css:24` | `.app-topbar`（頂欄） | 明確「頂欄」材質，非層 |
| `shared.css:1316` | `.wizard__top` | 註解自承「與 .app-topbar 角色完全相同」|
| `shared.css:1489` | `.wizard__bottom` | 註解自承「殼層 chrome」，與頂欄成對 |
| `ds-components/canvas-stage.css:303` | `.canvas-stage__pick--active/:hover` | 明確 hover/active 態 |
| `ds-components/canvas-stage.css:175` | 浮動膠囊 | 註解自承「浮在照片上」的材質語言 |
| `ds-components/filter-tabs.css:83` | `.filter-tabs__item--active:hover` | hover 態 |
| `ds-components/page-intro.css:141` | `.page-crumb__back:hover` | hover 態 |
| `ds-components/product-list.css:90` | `.product-list__row:hover` | 註解原句即「浮起 hover」——與任務排除例逐字相符 |
| `ds-components/project-list.css:77` | `.project-list__row:hover` | 同上，「浮起 hover」|
| `ds-components/info-banner.css:16` | `.info-banner` | 註解自承「不是浮起的卡…只要材質語言」|

**不確定（batch 3）——3 支：**

| 檔案:行號 | 選擇器 | 疑點 |
|---|---|---|
| `ds-components/list-toolbar.css:37` | 工作列 | 註解比照通知條（卡面等效），但功能上是 sticky 工具列 chrome，兩種讀法都說得通 |
| `ds-components/list-toolbar.css:296` | 貼頂列 | 同上，比照外殼玻璃 |
| `shared.css:1772` | `.detail-bar`（`var(--detail-bar-bg, var(--ztu-glass-bg))`） | detail 頁 sticky 頂列，用途像頂欄但走 fallback 語法 |

**文件/實驗頁（不列入任何 batch，非正式頁面消費）：** `design-tokens.html:99,137,156`、`lab-row-layers.html:216`、`lab-row-disclosure.html:229`、`lab-row-hover.html:133` 共 6 處。

### 第三組 A：`var(--nest-surface)`（4%）→ 候選 `--layer-2-surface`（8.5%，**有變動**）

共 16 支真消費者，**全部**帶有 2026-08-13「Q66 全站落地」的明確註解，語意上百分之百是「疊在卡上的分組面」，唯一問題是目前吃的是舊 4% 濃度：

| 檔案:行號 | 選擇器 | 現值 |
|---|---|---|
| `ds-components/bundle-editor.css:508` | `.bd-sec` | `background: var(--nest-surface)`（＋`border-color: var(--nest-line)`）|
| `ds-components/funding-panel.css:30` | `.funding-panel` | 同上 |
| `ds-components/funding-panel.css:132` | `.card .funding-panel--card, .payout-dialog .funding-panel--card` | 同上 |
| `ds-components/message-modal.css:117` | `.msg-schedule` | 同上 |
| `ds-components/payout-modal.css:27` | `.payout-bank-card` | 同上 |
| `ds-components/payout-modal.css:42-45` | `.payout-bank-card--selected` | 同上（含 inset 999px 疊色技巧）|
| `ds-components/payout-modal.css:86` | `.payout-bank-empty` | 同上 |
| `ds-components/payout-modal.css:240` | `.payout-bank-option` | 同上 |
| `ds-components/payout-modal.css:288` | `.payout-summary` | 同上 |
| `ds-components/picker.css:32` | `.card .picker, .payout-dialog .picker` | 同上 |
| `ds-components/pickup.css:24` | `.scanner-access` | 同上 |
| `ds-components/review-status.css:39` | `.review-status` | 同上 |
| `ds-components/store-settings.css:107` | `.ss-status` | 同上 |
| `create-bundle.html:54` | 內嵌 `<style>` 摘要框 | 同上 |
| `create-bundle.html:72` | `.cb-summary` | 同上 |
| `design-layers.html:155` | `.now-nest` | 同上（本頁本身就是層級系統的今日示範頁，建議連同它一起確認）|

判定：**全部遷，但列為第二批（有變動）**——填色從 4% 提到 8.5%，會比目前亮。每一支同時帶的 `border-color`/`box-shadow: inset ... var(--nest-line)` 可以同批一起把 token 名換成 `--layer-2-line`（這部分零變動，因為 `--layer-2-line` 本身就是 `--nest-line` 的別名）。

**受影響頁面（依 class 實際被哪些頁引用回推，未逐頁截圖驗證）：**
- `bundle-detail.html`、`create-bundle.html`（`.bd-sec`）
- `project-detail.html`（`.funding-panel`／`.funding-panel--card`、`.review-status`）
- `earnings.html`、`earnings-ztor.html`（`.payout-bank-*`、`.payout-summary`）
- `admin-creator-events.html`（`.picker` 卡內態）
- `pickup.html`、`pickup-detail.html`（`.scanner-access`）
- `e-shop.html`、`fans-crm.html`（`.msg-schedule` 所在的訊息彈窗）
- `store-settings.html`（`.ss-status`）
- `design-layers.html`（本身是示範頁）

### 第三組 B：`var(--ztu-film)`（8.5%）→ 候選 `--layer-2-surface`

共 159 處出現（73 個檔案），**絕大多數是凹槽／控件底／hover 態，不遷**——抽查涵蓋 badge、avatar、icon 圓底、縮圖占位、進度條軌道、code chip、table hover、sidebar hover 等約 40+ 支，全部落在「控件填色，不是分組面」，其中多支還帶有「舊值 var(--muted)／var(--card)」的補漏註解，代表它們原本就是小元件底色被就近換成薄膜值，不是層級語意。

只找到 **1 支**明確符合「疊在卡上的分組面」語意、且與已遷移的 `.card--muted` 是同一件事：

| 檔案:行號 | 選擇器 | 現值 | 判定 |
|---|---|---|---|
| `ds-components/kpi.css:322` | `.kpi.card--muted` | `background: var(--ztu-film)` | **遷（零變動）**——註解明寫「對齊 .card--muted 的薄膜值」，`.card--muted` 本身已經吃 `--layer-2-surface`，這支理應同步，純改名不改值 |

⚠ 覆蓋說明：159 處裡只重點抽查與逐一讀了約 40 支帶關鍵字（`分組面`／`巢狀層`／`Q66`／`第二層`）的檔案，其餘約 120 支未逐支開讀，是基於「background 有明確填色 + 選擇器命名為 icon/avatar/thumb/badge/track/chip/hover」等強烈控件特徵做的批次排除，未列出逐行明細。如果要 100% 把關，需要對這 120 支再開一輪逐行讀。

### 第四組：`var(--border)` → 候選 `--layer-line`

`var(--border)` 消費者數量龐大（統計到 413 處），不逐一列出（依指示：其餘不遷、不用列）。改用「找線索」的方式定位候選：先找站上既有的 **Q24（2026-07-21 已裁決的『卡內第三層只用 1px 邊框、不疊填色』規則）** 相關註解，再核對每支是否真的無填色。

**判定為 L3（遷，零變動）——9 支：**

| 檔案:行號 | 選擇器 | 現值 | 判定依據 |
|---|---|---|---|
| `ds-components/bundle-editor.css:521` | `.bd-sec .bd-pick` | `background: transparent; border: 1px solid var(--border)` | 註解明寫「L3：…改用線框表達層級——Q24 的第三階規則…Q24 明訂 L3 就用 --border」，最直接的證據 |
| `ds-components/bundle-editor.css:606` | `.bd-tbl` | `border: 1px solid var(--border)`，無填色 | 註解明寫「整張表加一圈外框（L3 用 --border，同 .bd-pick）」|
| `ds-components/variant-builder.css:208` | `.variant-option` | `background: transparent; border: 1px solid var(--border)` | 註解明寫「2026-07-21 Q24：L3 靠邊框，不疊填色」|
| `ds-components/variant-builder.css:122` | `.option-set__preset` | `background: transparent; border: 1px solid var(--border)` | 同檔同一組 Q24 語彙的相鄰規則，樣式一致 |
| `ds-components/variant-builder.css:263` | `.variant-table-wrap` | `border: 1px solid var(--border)`，無填色 | 與 `.bd-tbl` 同一種「表格外框」模式 |
| `ds-components/control-row.css:44` | `.control-group` | `box-shadow: inset 0 0 0 1px var(--border)`，無填色 | card-group.css 文件明寫這是「卡內分組三級制」第 2 級：「1px 線框圈出來、不加填色層」|
| `ds-components/control-row.css:25` | `.control-row` | `box-shadow: inset 0 0 0 1px var(--border)`，無填色 | 與 `.control-group` 同角色（註解：「兩者是同一個角色」）|
| `ds-components/album-tracks.css:40` | `.album-tracks__list` | `box-shadow: inset 0 0 0 1px var(--border)`，無填色 | 純框線包住一組列表，同一模式 |
| `ds-components/row-disclosure.css:316` | `.rowdis__frame:has(...)` | `box-shadow: inset 0 0 0 1px var(--border)`，無填色 | 展開態的分組框，同一模式 |

**不確定（batch 3）——2 支，模式相符但缺乏明確註解佐證：**

| 檔案:行號 | 選擇器 | 現值 | 疑點 |
|---|---|---|---|
| `ds-components/lock-sets.css:61` | `.lockset` | `border: 1px solid var(--border)`，無填色 | 樣式與其餘 L3 候選一致，但無 Q24／L3 相關註解佐證，也可能單純是「表格外框」（卡片外框排除類）|
| `ds-components/scanner.css:156` | `.scanner-count` | `border: 1px solid var(--border)`，無填色 | scanner.html 是獨立全螢幕工具頁，不確定是否適用「卡內第三層」模型 |

**明確排除、不遷的類型舉例（用於說明判準，未逐支列出）：** `.group-divider`／`.card-head__rule`（分隔線，非線框）、`.control-group__body`（`border-top` 單邊分隔線）、`.badge--outline`／`.msg-token`／`.fg-perk`（本身帶填色的徽章/藥丸）、`.pickup-select__row`／`.scanner-demo`（帶 `background:var(--card)` 的獨立卡外框）、`.sce__add`／`.variant-option__add-value`（虛線「新增」按鈕）、`.pickup-detail__value` 類輸入框底（`--input-surface`，凹槽/控件底範疇）。

---

## 分批執行建議

**第一批（零視覺變動，可直接批次改名）——共 40 支**
- 第一組：1 支（`shared.css:250`）
- 第二組：29 支（見上表 L1 清單）
- 第三組 B：1 支（`kpi.css:322`）
- 第四組：9 支（見上表 L3 清單，含各自搭配的 `--border`→`--layer-line`）

**第二批（有視覺變動，需使用者看過再動）——共 16 支，全部是 `--nest-surface`→`--layer-2-surface`（4%→8.5%）**
影響頁面：`bundle-detail.html`、`create-bundle.html`、`project-detail.html`、`earnings.html`、`earnings-ztor.html`、`admin-creator-events.html`、`pickup.html`、`pickup-detail.html`、`e-shop.html`、`fans-crm.html`、`store-settings.html`、`design-layers.html`。同批可以把這 16 支各自搭配的 `--nest-line`→`--layer-2-line` 一起做（那部分零變動）。

**第三批（不確定，需要使用者裁決）——共 9 支**
- 第一組：4 支（ztu-canvas 的材質/效果用法：`canvas-stage.css:37-38`、`fan-store.css:162`、`detail-sheet.css:55`、`preview-column.css:103`）
- 第二組：3 支（`list-toolbar.css:37`、`list-toolbar.css:296`、`shared.css:1772`）
- 第四組：2 支（`lock-sets.css:61`、`scanner.css:156`）

---

## 覆蓋範圍

**已搜尋：** `ds-components/*.css`（全部 168 檔）、`shared.css`、`partials/*.js`（全部 29 檔）、`js/*.js`（全部 53 檔）、根目錄所有 `*.html` 的內嵌 `<style>`（含 `design-system.html`、`design-tokens.html`、`design-layers.html`、`lab-*.html` 等實驗/文件頁）。四組分別用 `grep -rn` 抓出所有 `var(--ztu-canvas)`／`var(--ztu-glass-bg)`／`var(--nest-surface)`／`var(--ztu-film)`／`var(--border)` 字面出現，逐一或抽樣讀取上下文與註解判定。

**未逐支覆蓋（誠實說明限制）：**
- 第三組 B（`--ztu-film`，159 處）只重點讀了約 40 支帶關鍵字的規則，其餘約 120 支是依「有填色＋控件命名特徵」批次排除，未逐行開讀確認。
- 第四組（`--border`，413 處）完全沒有逐行讀過；候選清單是靠「找 Q24 相關註解」＋「找 `inset 0 0 0 1px var(--border)` 且同規則無填色」兩種線索定位出來的，可能有漏網的、沒有留註解的 L3 候選。
- `assets/`、`docs/`、`references/`、`screenshots/`、`scripts/` 目錄未搜尋（非樣式消費來源，不含 CSS/HTML 頁面）。
- `funding-test/`、`golive-4step/` 子目錄未搜尋（未在需求列出的搜尋範圍內）。
