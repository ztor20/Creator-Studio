# Creator Studio — 本機工作規則（不進 monorepo）

這份檔案刻意放在 `creator-studio-source/`，**不在 `ztor-creator-studio/` 裡面**：
那個目錄是 git subtree，`collab.sh` 會把它整包灌進 monorepo 並開 PR。放在這一層
＝只影響本機的 AI 工作方式，不會變成協作者的規範、也不會出現在任何 PR。

網站本身的共編鐵律（發版流程、`pull.sh` / `collab.sh`、版本字串）仍以
`ztor-creator-studio/CLAUDE.md` 為準，本檔不覆寫那些。

---

## 不做行動版（2026-07-29 使用者裁示）

**Creator Studio 是桌機產品。不要檢查、不要驗證、不要優化行動版。**

- 不 resize 到 mobile／tablet 斷點截圖，不做窄版走查，不為窄版寫「修好它」的迭代。
- 不主動把「行動版沒對齊／會溢出」列成待辦、findings 或建議。
- 驗收只在桌機視窗做（1280–1440 寬即可）。

理由：這個介面的使用場景是創作者坐在電腦前經營自己的事業——上架商品、看報表、
管理粉絲、剪素材。行動版不在產品範圍內，為它做走查與修正是純粹的 token 消耗。

允許但不必驗證：元件如果順手帶了 `@media` 窄版規則，可以留著（寫都寫了，成本已付），
**但不要為了它再開一輪檢查**。新元件也不必刻意補窄版規則。

例外：使用者明確說「這頁要看手機」時才做，而且只做那一頁。
（`scanner.html` 本來就是手機專用的獨立頁，屬於它自己的規格，不受本條限制。）
